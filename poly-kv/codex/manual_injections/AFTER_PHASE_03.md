# Manual Phase-Boundary Injection

Paste this into Codex before allowing the next phase.

Stop before proceeding.

Revalidate with exact file paths and commands:

1. Current source-of-truth owner for every touched concept.
2. No duplicate abstraction or shadow implementation was introduced.
3. No silent schema widening, shape coercion, hidden fallback, or fake compatibility layer was introduced.
4. Every material operation added or changed emits/returns the required receipt/artifact or is explicitly non-material.
5. Exact fallback remains available for every compressed alpha fixture.
6. Optional adapters do not claim compatibility unless the external API was inspected and compiled.
7. Tests/fixtures/assertions exist for new behavior.
8. Any failed/skipped validation is recorded as blocker, debt, or explicit non-goal.
9. Scope exclusions still hold: no governor, no scr-runtime, no semantic-memory/Gloss/Recall/AiDENs/ClaimLedger integration.
10. Rollback path remains clear.

If any item cannot be proven, stop and repair or report. Do not proceed on momentum.
