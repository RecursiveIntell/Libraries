# Phase 07 — Final audit

Produce `.codex-runs/<run-id>/final_audit_report.md` with:

- changed files
- commands run
- package output path
- manifest path
- root files before/after cleanup
- archive manifest path
- proof `_native.pyi`, `py.typed`, command evidence are included
- all failed/skipped checks with reasons
- rollback plan
- remaining risks
