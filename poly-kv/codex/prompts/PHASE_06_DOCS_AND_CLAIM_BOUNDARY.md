# Phase 06 — Docs and claim boundary

Update docs to state:

- z.py now archives package residue before packaging.
- context packages include Python typing markers and command evidence.
- source package history lives under `docs/source-packages/archive` unless explicitly included.
- package archive history is excluded by default from normal context zips.

Do not add product-performance claims.
