# Changelog

## [Unreleased]

### Added
- `.kind()` method on error types for stable programmatic matching (PRIMITIVES_CONTRACT S2)
- `.kind()` method on `digest.rs` error types
- `.kind()` method on `trace.rs` error types
