# P05 — Durable execution evidence ledger, receipt store, and transactional outbox

## Phase

Foundation / evidence

## Purpose

Replace in-memory-only receipts with durable append-only execution evidence and same-transaction outbox semantics.

## Why this pass exists

In-memory receipt ledgers are test helpers, not evidence. Execution evidence is the control-plane frontier: retries, route, deadline, queue hop, budget, degradation, and tool outcomes are meaning-bearing.

## Depends on

P04

## Primary crates / surfaces

- `aidens-receipts`
- `aidens-contracts`
- `aidens-cli`
- `aidens-runner`
- `aidens-daemon-kit`

## Artifact families introduced or hardened

- canonical library receipt log/sink
- `PoisonReceiptRecordV1`
- `ExecutionLineageGraphV1`

## Implementation steps

1. Use canonical library receipt payloads and sinks; do not create an AiDENs envelope DTO.
2. Implement durable NDJSON receipt log over canonical payloads.
3. Do not add an AiDENs outbox abstraction for receipt truth.
4. Add CLI: receipts list, receipts inspect, receipts export, receipts verify-digest.
5. Link AiDENs display reports to canonical tool, Forge, and control receipts.

## Acceptance gates

- A run can be inspected after process restart via durable store.
- Receipt digests are stable across pretty/compact JSON representation.
- Every provider/tool/boundary/permit failure has an emitted receipt or explicit test proving no durable store was configured and receipt-level is minimal.

## Required tests

- Unit tests for all new artifact constructors and policy decisions.
- Integration tests covering the pass's primary CLI/runtime behavior.
- Golden fixture tests for any schema- or receipt-bearing artifact.
- Regression tests for every bug or counterexample found during the pass.

## Required command gate

```bash
cargo fmt --all --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
bash scripts/verify.sh
```

If a command cannot run in the local environment, Codex must record the exact command and reason in the handoff packet.

## Forbidden shortcuts

- Do not keep “durable” evidence only in Vec/Mutex.
- Do not export receipts by scanning mutable tables without outbox identity.

## Handoff output

At pass completion, create or update:

- pass completion section in `STATUS.md` or equivalent;
- issue matrix rows marked done/blocked/deferred;
- receipts/fixtures/schemas generated by this pass;
- `CodexPacketV1` or a markdown handoff with commands run, failures, files changed, and unresolved blockers.
