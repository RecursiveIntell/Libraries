# Phase 5 — CI, evidence, lint, documentation truth

Issues: `CI-001`, `LINT-001`, `EVD-001`, `DOC-001`.

Outputs: deterministic workspace inventory; complete feature/platform matrix; lint inheritance;
read-only verify; separate evidence record; source/environment-bound receipts; required CI checks;
claims manifest and conservative nonclaims.

Fresh hostile reviewer gets current tree, original issues, receipts/logs, migration/rollback—not
agent conclusions.

Exit: all P0/P1 closed; every required command passed; tree clean; evidence consistent/bound; no
release blocker from independent audit.
