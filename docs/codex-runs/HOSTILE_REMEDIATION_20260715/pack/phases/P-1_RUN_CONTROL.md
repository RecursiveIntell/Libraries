# Phase -1 — Run control and baseline

Close `CTRL-001`.

1. Verify hashes and install immutable run pack.
2. Add one active-run pointer; preserve prior packs.
3. Create run state, decisions, risks, path locks.
4. Generate workspace inventory and baseline receipts.
5. Reconcile every issue locator.
6. Create integration branch/worktrees.

Exit: no semantic source changes; baseline is clean or explicitly blocked; all P0 tasks bind to the
actual integration commit.
