# Hostile audit findings carried into remediation

P0: agent-graph converts ordinary errors to Complete; forge-pilot governance absence/error can Allow;
scr-runtime-compression returns encoded bytes as exact decoded data and ignores dispatch.

P1: stack-ids is permissive rather than authoritative; competing UUID/ULID/local IDs remain; digest
composition has separator injection; scope conversion drops dimensions; claim-ledger drops malformed
records and lacks a head anchor; codec contracts/profiles/metrics have multiple owners; batch queue
claim/completion is not atomic; semantic search can silently omit corrupt rows; CI/evidence covers
partial surfaces and disagrees; quantitative docs are not uniformly receipt-bound.

The machine-readable issue matrix is the acceptance authority.
