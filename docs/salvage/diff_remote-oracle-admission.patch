diff -ruN ../Libraries2/remote-oracle-admission/CHANGELOG.md remote-oracle-admission/CHANGELOG.md
--- ../Libraries2/remote-oracle-admission/CHANGELOG.md	2026-03-26 12:03:56.464082540 -0500
+++ remote-oracle-admission/CHANGELOG.md	2026-03-26 08:13:24.631789659 -0500
@@ -1,5 +1,6 @@
 # Changelog
 
 ## [Unreleased]
-### Changed
-- Path dependencies normalized to `../crate-name` format
+
+### Added
+- README.md
diff -ruN ../Libraries2/remote-oracle-admission/Cargo.toml remote-oracle-admission/Cargo.toml
--- ../Libraries2/remote-oracle-admission/Cargo.toml	2026-03-14 10:31:06.000000000 -0500
+++ remote-oracle-admission/Cargo.toml	2026-03-30 22:31:16.765504548 -0500
@@ -11,3 +11,7 @@
 serde = { workspace = true, features = ["derive"] }
 stack-ids = { path = "../stack-ids" }
 attestation-exchange = { path = "../attestation-exchange" }
+
+# LIB-005: inherit workspace lint policy
+[lints]
+workspace = true
diff -ruN ../Libraries2/remote-oracle-admission/README.md remote-oracle-admission/README.md
--- ../Libraries2/remote-oracle-admission/README.md	2026-03-26 12:03:24.862176537 -0500
+++ remote-oracle-admission/README.md	2026-03-26 07:57:23.157739504 -0500
@@ -2,23 +2,27 @@
 
 Typed remote oracle admission contracts for lease, result, replay, and re-admission artifacts.
 
-## Example
+## Usage
 
-```rust
-use remote_oracle_admission::{RemoteOracleLeaseV1, V25ConstitutionCitation};
+```sh
+cargo add remote-oracle-admission
+```
 
-let lease = RemoteOracleLeaseV1::new(
-    lease_id, "oracle:trusted-remote", citation,
-    &["artifact:v1"], trust_root_set_id,
-)?;
-lease.validate()?;
+```rust
+use remote_oracle_admission::{RemoteOracleLeaseV1, RemoteSliceRequestV1, RemoteSliceResultV1};
 ```
 
 ## Ecosystem
 
-- **stack-ids**: All artifact IDs plus `V25ConstitutionCitation`
-- **attestation-exchange**: Provides attestation types used in admission flows
+**Depends on:**
+- `stack-ids` -- identity primitives (lease, slice, replay, and revocation IDs)
+- `attestation-exchange` -- attestation envelope types
+
+**Depended on by:**
+- `contract-schema-gen`
 
 ## stack-ids integration
 
-Fully integrated. `V25ConstitutionCitation` is re-exported from `stack-ids`.
+All artifact identifiers (`RemoteOracleLeaseId`, `RemoteSliceRequestId`,
+`RemoteSliceResultId`, `CrossRuntimeReplayTicketId`, etc.) are sourced from
+`stack-ids`. The `V25ConstitutionCitation` type is re-exported directly.
diff -ruN ../Libraries2/remote-oracle-admission/src/lib.rs remote-oracle-admission/src/lib.rs
--- ../Libraries2/remote-oracle-admission/src/lib.rs	2026-04-12 02:24:52.975368150 -0500
+++ remote-oracle-admission/src/lib.rs	2026-03-30 15:58:14.991338517 -0500
@@ -75,6 +75,7 @@
 }
 
 #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, JsonSchema)]
+#[serde(rename_all = "snake_case")]
 pub enum RemoteDisclosureClassV1 {
     #[serde(rename = "non-sensitive")]
     NonSensitive,
@@ -89,6 +90,7 @@
 }
 
 #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, JsonSchema)]
+#[serde(rename_all = "snake_case")]
 pub enum AttestationReplayImpactV1 {
     #[serde(rename = "replay ticket unchanged")]
     ReplayTicketUnchanged,
diff -ruN ../Libraries2/remote-oracle-admission/tests/v25_local_constitution_refs.rs remote-oracle-admission/tests/v25_local_constitution_refs.rs
--- ../Libraries2/remote-oracle-admission/tests/v25_local_constitution_refs.rs	2026-04-12 02:24:52.976368149 -0500
+++ remote-oracle-admission/tests/v25_local_constitution_refs.rs	2026-05-12 20:03:21.222453664 -0500
@@ -1,3 +1,5 @@
+#![allow(clippy::expect_used)]
+
 use remote_oracle_admission::{
     CrossRuntimeReplayTicketV1, LocalAdmissionRecommendationV1, RemoteDisclosureClassV1,
     RemoteExactnessClassV1, RemoteSliceRequestV1, RemoteSliceResultV1, ReplayFailureBehaviorV1,
diff -ruN ../Libraries2/remote-oracle-admission/tests/validation_tests.rs remote-oracle-admission/tests/validation_tests.rs
--- ../Libraries2/remote-oracle-admission/tests/validation_tests.rs	2026-03-24 19:40:17.087286648 -0500
+++ remote-oracle-admission/tests/validation_tests.rs	2026-05-12 20:03:21.223596400 -0500
@@ -1,3 +1,5 @@
+#![allow(clippy::expect_used)]
+
 use remote_oracle_admission::{
     AttestationReplayImpactV1, AttestationSupersessionV1, CrossRuntimeReplayTicketV1,
     LocalAdmissionRecommendationV1, RemoteDisclosureClassV1, RemoteExactnessClassV1,
