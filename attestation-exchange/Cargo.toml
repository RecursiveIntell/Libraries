[package]
name = "attestation-exchange"
version = "0.1.0"
edition = "2021"
rust-version = "1.75.0"
description = "Typed attestation exchange contracts for envelope, trust-root, and transparency artifacts"
license = "MIT"

[dependencies]
schemars = { workspace = true }
serde = { workspace = true, features = ["derive"] }
stack-ids = { path = "../stack-ids" }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

# LIB-005: inherit workspace lint policy
[lints]
workspace = true
