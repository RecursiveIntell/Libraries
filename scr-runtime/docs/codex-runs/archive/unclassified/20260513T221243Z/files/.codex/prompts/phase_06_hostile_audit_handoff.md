# Phase 06 — Hostile Auditor Handoff

Goal: produce final completion report and an audit handoff.

Required report:

```text
docs/PASS_CL_P0_COMPLETION_AUTOMATED_PHASES.md
```

Required content:
- PASS / PARTIAL / FAIL;
- changed files;
- exact commands run;
- exact command results;
- proof that automated phase injection replaced manual injection;
- proof that `.codex/` is active and packaged;
- fresh-unzip proof or explicit blocker;
- unresolved risks;
- next recommended pass.

Do not claim success if any release gate failed.
