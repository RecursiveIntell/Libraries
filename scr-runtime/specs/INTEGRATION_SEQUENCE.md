# Integration Sequence

Do not skip sequence.

```text
P0A — deterministic reference kernel over audit fixtures
P0B — audit/Codex repair packet generator
P1  — memory governance adapter
P2  — retrieval/widening/degradation adapter
P3  — tool exposure/permit adapter
P4  — Recall/AiDENs integration
P5  — UI disclosure and operator override queue
P6  — learned calibration only from verified historical receipts
```

P0A must not integrate with Recall, AiDENs, memory, retrieval, or tools.
