Manual invariant injection before final response:

Do not answer with completion until you can paste exact evidence:
1. changed files,
2. commands run,
3. passing checks,
4. failing checks,
5. skipped checks with reasons,
6. final docs created,
7. rollback plan,
8. unresolved risks,
9. fresh-unzip result if package generated.

If any required command did not run, say exactly why and do not claim release completion.
