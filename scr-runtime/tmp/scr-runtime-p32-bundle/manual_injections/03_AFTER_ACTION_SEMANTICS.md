Manual invariant injection after Phase 03:

Prove SCR now evaluates proposed action/effect materially:
1. show code path where action/effect affects axes/candidates,
2. show tests with same signals but different action/effect producing different receipts,
3. show grep proving no opaque evidence ref signal scanning in evaluator,
4. show fixture adapter as only legacy signal translation point.

If evaluator still resolves mostly from ref tokens, stop.
