Manual invariant injection after Phase 00:

Before implementation, prove with file paths and command outputs:
1. exact repo root,
2. current branch/commit/dirty state,
3. selected run id,
4. existing stale P30/P31 surfaces,
5. whether Rust toolchain exists,
6. where command receipts will be written.

If you cannot prove these, stop and repair preflight. Do not code yet.
