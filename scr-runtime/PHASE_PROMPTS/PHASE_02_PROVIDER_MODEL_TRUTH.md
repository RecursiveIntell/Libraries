# PHASE 02 PROVIDER MODEL TRUTH

Implement provider-specific refresh, stale/missing model state, and no unknown-model-to-Ollama fallback. Add tests/static proof.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_02_provider_model_truth --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
