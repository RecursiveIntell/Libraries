# PHASE 08 MEDIA FINALIZATION EXTERNAL TOOLS

Fix image/video finalization semantics, budget/preempt media jobs, add external tool timeout/cache.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_08_media_finalization_external_tools --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
