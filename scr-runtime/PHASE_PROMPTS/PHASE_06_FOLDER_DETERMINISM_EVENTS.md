# PHASE 06 FOLDER DETERMINISM EVENTS

Sort folder traversal and emit scan-start/failed/empty/truncated/completed events with frontend handling.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_06_folder_determinism_events --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
