# PHASE 07 FRONTEND BATCHING LARGE LISTS

Implement debounced selection persistence, batched refresh, memoized source ID sets, and bounded list rendering.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_07_frontend_batching_large_lists --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
