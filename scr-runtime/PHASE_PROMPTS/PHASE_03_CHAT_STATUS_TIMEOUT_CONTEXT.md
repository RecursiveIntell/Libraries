# PHASE 03 CHAT STATUS TIMEOUT CONTEXT

Implement chat:status lifecycle, first-token/idle timeouts, dynamic num_ctx, and truncated stream disclosure.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_03_chat_status_timeout_context --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
