# PHASE 09 VALIDATION REVIEW PACKAGE

Run builds/tests/static validator, perform review, create final receipt and runtime-debug package.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_09_validation_review_package --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
