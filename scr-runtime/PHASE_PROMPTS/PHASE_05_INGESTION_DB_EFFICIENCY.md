# PHASE 05 INGESTION DB EFFICIENCY

Implement batch add/delete paths, transactional chunk replacement/batch insert, and header-only summary discovery.

At the end of this phase, run:

```bash
python3 scripts/p32r3_phase_gate.py --run-id P32R3 --phase phase_05_ingestion_db_efficiency --status pass
```

If blocked, use `--status blocked --note "<reason>"` and stop.
