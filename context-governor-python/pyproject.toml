[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "context-governor"
version = "0.1.0"
description = "Python bindings for context-governor"
requires-python = ">=3.9"

[tool.maturin]
module-name = "context_governor._native"
python-source = "python"
