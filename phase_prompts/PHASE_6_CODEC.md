# Phase 6 — Fixed-Rate Codec

Implement encode/decode, fp16 norm, fixed-rate index packing, inverse rotation, and failure checks.

Gate: roundtrip finite, corrupt states fail closed, arbitrary N supported.
