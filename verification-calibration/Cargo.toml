[package]
name = "verification-calibration"
version = "0.1.0"
edition = "2021"
rust-version = "1.75.0"
description = "Canonical verification calibration and abstention artifacts"
license = "MIT"

[dependencies]
schemars = { workspace = true }
serde = { workspace = true, features = ["derive"] }
stack-ids = { path = "../stack-ids" }
verification-control = { path = "../verification-control" }

[dev-dependencies]
serde_json = { workspace = true }
