# Libraries2 — Workspace Snapshot

**Date**: 2026-04-11
**Source workspace**: `/home/sikmindz/Coding/Libraries2`
**Version control**: NONE (not a git repository)

---

## 1. Identity & Purpose

**Libraries2** is a sister/parallel workspace to `/home/sikmindz/Coding/Libraries`. It exists as a **staging and planning workspace** for the Utility Libraries Ecosystem — a collection of ~14 Rust crates plus one TypeScript package forming the utility layer for a local-first AI agent runtime.

**Distinction from Libraries**:

- **Libraries** (`/home/sikmindz/Coding/Libraries`) — canonical production source; stable, shipped, hardened; under git
- **Libraries2** (`/home/sikmindz/Coding/Libraries2`) — frozen staging snapshot used for audit follow-up and refactoring planning; **not a git repo**

Both share the same fundamental architecture and crate roster, but Libraries2 includes:
- Freshly generated governance docs (`CLAUDE.md`, `MASTER_ISSUE_MATRIX.md`, `HOSTILE_AUDIT_REPORT.md`, all dated 2026-03-26)
- The 43 KB `Libraries2.md` comprehensive crate catalog (regenerated 2026-04-04)
- Symlinks to external dependencies (`llm-tool-runtime`, `.parser-lib`)
- Documentation overlays and conformance gate scaffolds

Per `CLAUDE.md`: "A collection of reusable Rust crates and one TypeScript package that form the utility layer for a local-first AI agent runtime. The crates are designed to compose: they share identity primitives via `stack-ids`, use consistent error patterns, and follow the same API conventions."

---

## 2. Workspace Layout

### Workspace members (build via `cargo check --workspace`)

| # | Crate | Version | Role |
|---|---|---|---|
| 1 | `stack-ids` | 0.2.0 | Identity primitives — 217 unique ID newtypes, `TraceCtx`, `ContentDigest`, governance types |
| 2 | `job-queue` | 0.2.0 | SQLite-backed queue (~3.5 k LOC) — atomic claims, retry lineage, heartbeat reclaim |
| 3 | `agent-graph` | 0.2.0 | Graph orchestration (~4.7 k LOC) — DAG execution, parallel fan-out, checkpointing, LangGraph-style |
| 4 | `ComfyUI-RS` | 0.2.0 | ComfyUI client (~1.7 k LOC) — REST + WebSocket, workflow builder |
| 5 | `Ollama-Vision-RS` | 0.2.0 | Vision toolkit (~671 LOC) — tagging/captioning, robust output parsing |
| 6 | `discovery-portfolio` | 0.1.0 | Experiment portfolio types (~415 LOC) |
| 7 | `spec-execution` | 0.1.0 | Spec/proof surface (~696 LOC) |
| 8 | `federated-settlement` | 0.1.0 | Cross-runtime treaty surface (~625 LOC) |
| 9 | `attestation-exchange` | 0.1.0 | Attestation envelope contracts (minimal) |
| 10 | `remote-oracle-admission` | 0.1.0 | Oracle admission contracts (minimal) |

### Excluded from workspace (require external/Tauri build env)

| Crate | Version | Reason |
|---|---|---|
| `AI-Batch-Queue` | 0.2.0 | Model-aware batching (~1.8 k LOC); requires Tauri 2 |
| `Tauri-Queue` | 0.3.0 | Tauri event bridge (~1.5 k LOC); requires Tauri 2 |
| `LLM-Pipeline` | 0.2.0 | LLM workflows (~9.1 k LOC); requires `llm-tool-runtime` + `llm-output-parser` |
| `constraint-compiler` | 0.1.0 | Projection compiler; missing 3 path deps |
| `profile-runtime` | 0.1.0 | Profile composition; missing 5 path deps |
| `demo-tauri-libraries` | — | Demo app; requires Tauri + UI toolchain |

### Symlinks (external references)

| Symlink | Target | Purpose |
|---|---|---|
| `llm-tool-runtime` | `/home/sikmindz/Coding/Libraries/llm-tool-runtime` | LLM execution backend abstraction; symlinked to canonical Libraries copy |
| `.parser-lib` | `/home/sikmindz/Coding/Gloss/src-tauri/vendor/llm-output-parser` | Vendored LLM output parser from Gloss UI codebase |

### Non-crate directories

- `overlay/` — alternative governance docs (AGENTS.md, PROMPT.md, MASTER_ISSUE_MATRIX.md variants)
- `repo_overlay/conformance/p1-p6/` — conformance test gates
- `plans/` — project plans and execution framework
- `prompts/` — prompts directory for Claude Code runs
- `docs/` — archive docs, control-plane specs, current state assessments
- `examples/` — Cargo examples
- `schemas/`, `schemas.generated/` — JSON Schema artifacts
- `scaffolds/`, `snippets/`, `reference/`, `reference_stubs/` — template/reference code

---

## 3. Current Pass / Mission

Governance docs (all dated 2026-03-26):

| Doc | Purpose | Status |
|---|---|---|
| `CLAUDE_CODE_PROMPT.md` | 5-phase execution plan: normalize path deps → create workspace Cargo.toml → fix `cancel_job` TOCTOU → fix `AgentState` transactions → decompose `graph.rs` | OPEN — phases defined, no implementations yet |
| `MASTER_ISSUE_MATRIX.md` | 45 issues across 8 crates: 2 CRITICAL, 8 HIGH, 22 MEDIUM, 13 LOW. Categories: 10 coherence, 9 correctness, 6 architecture, 8 safety, 5 performance, 4 testing, 3 debt | OPEN — initial audit 2026-03-25 |
| `HOSTILE_AUDIT_REPORT.md` | Systematic file-by-file review. Overall 7.8/10. Strengths: correct concurrency in `job-queue`, `CoalescingEmitter`, `stack-ids` threading. Risks: path-dependency chaos, `agent-graph` monolith, `AgentState` lost-update, zero tests in `Tauri-React-Hooks` | CLOSED — findings actionable |

**Top 5 priority actions** (per the audit):

1. Normalize path dependencies + create unified workspace `Cargo.toml` (BLOCKING)
2. Decompose `agent-graph/src/graph.rs` (1539 lines → 4 files)
3. Fix `AgentState::transaction()` lost-update anomaly
4. Add tests for `Tauri-React-Hooks` (zero coverage currently)
5. Fix `cancel_job()` TOCTOU race in `job-queue`

---

## 4. Build & Test State

**Buildability**:
- 10 workspace members compile clean with `cargo check --workspace`
- 6 excluded crates do NOT compile from Libraries2 root:
  - `AI-Batch-Queue`, `Tauri-Queue`, `LLM-Pipeline` — missing Tauri 2 build environment
  - `constraint-compiler` — missing 3 path deps (`forge-memory-bridge`, `recursive-kernel-core`, `semantic-memory-forge`)
  - `profile-runtime` — missing 5 path deps (`assurance-runtime`, `attestation-exchange`, `authority-delegation`, `continuity-runtime`, `verification-policy`)

**Path dependency chaos** (CRITICAL — PATH-1):
Two conflicting conventions exist in the 10 member crates:
- `job-queue/Cargo.toml` uses `../../Libraries/stack-ids`
- `AI-Batch-Queue/Cargo.toml` uses `../stack-ids` (correct for same-level siblings)
- `agent-graph/Cargo.toml` uses `../../Libraries/stack-ids` (inconsistent)

**No unified workspace `Cargo.toml`** (HIGH — PATH-2):
The current `/home/sikmindz/Coding/Libraries2/Cargo.toml` exists but **excludes** the Tauri crates and compiler crates rather than unifying them. This blocks:
- Unified dependency versions (crates could use different `serde`/`tokio` versions)
- `cargo test --workspace` validation across all 13 crates
- CI gates preventing path-dependency regressions

**Test coverage**:
- **`job-queue`**: 40+ test cases (state transitions, races, crash recovery) — 8.5/10
- **`agent-graph`**: ~2700 lines of tests across 11 test files — 7.5/10 (loses points for transaction anomaly)
- **`Tauri-React-Hooks`**: ZERO test files — HIGH-priority gap
- Thin crates (`discovery-portfolio`, `spec-execution`, etc.): 1–2 basic serde roundtrip tests each

**Conformance gates** (`repo_overlay/conformance/`): `p1-p6` directories exist but contents not examined.

---

## 5. Git/File State

**Not a git repository.** `git status` returns "fatal: not a git repository".

**File activity** (by mtime, most recent first):

- **2026-04-04 19:46** — `Libraries2.md` (43.5 KB) — full snapshot doc regenerated
- **2026-03-26 14:37** — `LLM-Pipeline/`, `agent-graph/` — recent updates
- **2026-03-26 12:33** — `CLAUDE_CODE_PROMPT.md`, `CLAUDE.md`, `HOSTILE_AUDIT_REPORT.md`, `MASTER_ISSUE_MATRIX.md` — audit docs locked
- **2026-03-26 12:03** — workspace crates touched (`remote-oracle-admission`, `attestation-exchange`, `federated-settlement`, `spec-execution`, `discovery-portfolio`, `Ollama-Vision-RS`, `ComfyUI-RS`, `Tauri-Queue`, `AI-Batch-Queue`, `job-queue`, `stack-ids`)
- **2026-03-26 08:44** — `libraries-source-clean-20260326.zip` (1.7 MB)
- **2026-03-25 11:01** — `libraries2(3-25-26).zip` (3.9 MB)

**Build artifacts present**:
- `AI-Batch-Queue/target/` — 3.7 GB (recent development)
- `LLM-Pipeline/target/` — 551 MB
- Various `tmp-cargo-target-*` directories (cleanup recommended)

**Backup zips**:
- `libraries-source-clean-20260326.zip` — latest clean snapshot (2026-03-26 08:44)
- `libraries-source-clean-20260325.zip` — prior day
- `libraries2(3-25-26).zip` — full workspace snapshot

---

## 6. Hostile Audit Summary (`HOSTILE_AUDIT_REPORT.md`)

**Overall ecosystem score: 7.8/10** — genuinely well-engineered, but critical gaps exist.

**Crate-by-crate scores**:

| Crate | Score | Top finding |
|---|---|---|
| `job-queue` | 8.5/10 | Production-grade lease mgmt (atomic claims, heartbeat reclaim); `cancel_job()` has TOCTOU race |
| `Tauri-Queue` | 8.0/10 | `CoalescingEmitter` solves event flooding correctly; emits failures silently |
| `LLM-Pipeline` | 8.0/10 | Sophisticated retry/parsing; `ExecCtx` timeout baked at build time (can't serve mixed latencies) |
| `agent-graph` | 7.5/10 | Ambitious execution model with state limits; `AgentState::transaction()` has lost-update anomaly + 1539-line monolith |
| `AI-Batch-Queue` | 7.5/10 | Model-aware reordering clever; global Mutex on jobs + split locks → contention under concurrent load |
| `constraint-compiler` | 6.5/10 | Clean design; cannot compile (missing 3 path deps) |
| `ComfyUI-RS` | 7.0/10 | Good error handling; unbounded image downloads + no `stack-ids` integration |
| `Ollama-Vision-RS` | 7.0/10 | Robust parsers; hardcoded 30 s timeout + no `stack-ids` |
| `Tauri-React-Hooks` | 7.0/10 | Correct async patterns; **zero tests** |
| Thin crates (`discovery-portfolio`, `spec-execution`, ...) | 6.5/10 | Well-structured types; minimal tests |
| `profile-runtime` | 6.0/10 | Cannot compile (missing 5 path deps) |

**Cross-cutting findings**:

| Severity | ID | Issue | Impact |
|---|---|---|---|
| **CRITICAL** | PATH-1 | Inconsistent path deps (`../../Libraries/` vs `../`) | Crates cannot compile from same workspace layout |
| **CRITICAL** | PR-1 | `profile-runtime` missing 5 deps | Crate non-functional in distribution |
| **HIGH** | PATH-2 | No workspace `Cargo.toml` | No shared dep resolution, no `cargo test --workspace` |
| **HIGH** | PATH-3 | `LLM-Pipeline` needs `llm-output-parser`, `llm-tool-runtime` | Crate excluded from workspace |
| **HIGH** | AG-1 | `AgentState::transaction()` lost-update | Concurrent state changes silently lost between `transaction()` and `commit()` |
| **HIGH** | AG-2 | `graph.rs` monolith (1539 lines) | Single file mixes builder, engine, scheduler, checkpoint, event emission |
| **HIGH** | ABQ-1, ABQ-2 | Lock contention in `AI-Batch-Queue` | Global Mutex on jobs + split locks in `mark_running()` |
| **HIGH** | TRH-1 | Zero tests in `Tauri-React-Hooks` | 518 LOC TypeScript with no regression protection |
| **MEDIUM** | JQ-1 | `cancel_job()` TOCTOU | Two-step SELECT+UPDATE allows race; error message can be stale |
| **MEDIUM** | TRACE-1 | `ComfyUI-RS`, `Ollama-Vision-RS` lack `stack-ids` | Trace lineage breaks at their boundary |

**Strongest aspects**:

1. `job-queue` `claim_with_lease()` → heartbeat → `reclaim_stale()` lifecycle (production-grade)
2. `CoalescingEmitter` in `Tauri-Queue` (event flooding + mutex poisoning recovery)
3. `stack-ids` canonical threading across the ecosystem (genuine differentiator)
4. `AgentState` with `RwLock`, lock timeouts, value/key limits, reducer composition
5. Size-bucketed ETA estimation in `AI-Batch-Queue`

---

## 7. Notable Artifacts

**Governance docs**:
- `CLAUDE.md` — architecture invariants, API conventions, testing requirements
- `CLAUDE_CODE_PROMPT.md` — 5-phase execution roadmap (~9.5 KB)
- `HOSTILE_AUDIT_REPORT.md` — full audit with crate-by-crate findings (~23 KB)
- `MASTER_ISSUE_MATRIX.md` — structured issue tracking (~10 KB, 45 issues)
- `Libraries2.md` — comprehensive crate catalog (43 KB, 862 lines)

**Backup & source zips**:
- `libraries-source-clean-20260326.zip` — latest clean snapshot (1.7 MB)
- `libraries-source-clean-20260325.zip` — prior snapshot (1.6 MB)
- `libraries2(3-25-26).zip` — full workspace (3.9 MB)

**Build artifacts** (cleanup recommended):
- `AI-Batch-Queue/target/` — 3.7 GB
- `LLM-Pipeline/target/` — 551 MB
- `tmp-cargo-target-*` directories

**Reference & examples**:
- `docs/archive/control-plane/` — historical control-plane architecture docs
- `examples/` — Cargo examples (15+ scenarios)
- `schemas/`, `schemas.generated/` — JSON Schema artifacts for types
- `prompts/` — prompts used in Claude Code sessions
- `plans/` — project execution plans
- `reference/`, `reference_stubs/` — template/reference code snippets

---

## 8. Symlink Relationships

Two symlinks, both pointing to external directories outside Libraries2:

| Symlink | Target | Purpose | Status |
|---|---|---|---|
| `llm-tool-runtime` | `/home/sikmindz/Coding/Libraries/llm-tool-runtime` | LLM execution backend abstraction (symlink to Libraries canonical) | Up-to-date (Cargo.toml exists in target) |
| `.parser-lib` | `/home/sikmindz/Coding/Gloss/src-tauri/vendor/llm-output-parser` | Vendored LLM output parser from Gloss UI codebase | Minimal (Cargo.toml + LICENSE only in target) |

**Why symlinks?**
- `llm-tool-runtime` is maintained in Libraries (canonical) — symlinked here to keep semantic memory backend unified
- `.parser-lib` is vendored in Gloss (Tauri UI app) — symlinked here to avoid duplication in `LLM-Pipeline` builds

---

## 9. Three-Workspace Topology

```
/home/sikmindz/Coding/
├─ Libraries/                   (CANONICAL PRODUCTION, git)
│  ├─ stack-ids/
│  ├─ job-queue/
│  ├─ agent-graph/
│  ├─ ... (~31 crates: shipped + extended)
│  ├─ llm-tool-runtime/
│  └─ Cargo.toml (full workspace)
│
├─ Libraries2/                  (STAGING SNAPSHOT, no git)
│  ├─ stack-ids/                  (parallel copy)
│  ├─ job-queue/
│  ├─ agent-graph/
│  ├─ ... (10 workspace members, 6 excluded)
│  ├─ llm-tool-runtime -> ../Libraries/llm-tool-runtime/   (symlink)
│  ├─ .parser-lib    -> ../Gloss/src-tauri/vendor/...      (symlink)
│  ├─ Cargo.toml                  (partial workspace)
│  ├─ CLAUDE.md, MASTER_ISSUE_MATRIX.md, HOSTILE_AUDIT_REPORT.md
│  └─ Libraries2.md               (comprehensive snapshot)
│
└─ Recall/                      (DOWNSTREAM TAURI APP, git)
   ├─ Tauri 2 desktop application
   ├─ Vendors deps from Libraries (path deps + selected vendoring)
   └─ The current main working directory
```

**Relationship**:

- Libraries2 is **NOT a fork or branch** — it's a staging/development snapshot of Libraries at a specific point in time (around the 2026-03-26 audit)
- Libraries2 is **frozen** — not under version control; clean-room copy used for analysis and planning
- Recall depends on Libraries (canonical), not Libraries2 — it pulls path deps from Libraries
- Libraries2 exists to:
  - Isolate audit findings (`HOSTILE_AUDIT_REPORT.md`, `MASTER_ISSUE_MATRIX.md`) without modifying canonical
  - Stage the 5-phase refactoring plan (`CLAUDE_CODE_PROMPT.md`) before applying to Libraries
  - Document the ecosystem comprehensively (`Libraries2.md`) without changing Libraries
  - Serve as a "state at 2026-03-26 audit" reference snapshot

**Direction of influence**:

- **Libraries → Libraries2** (one-way): canonical crates snapshot into Libraries2 when a major audit/governance doc is generated
- **Libraries2 → Libraries** (planned): fix recommendations from `HOSTILE_AUDIT_REPORT` + `MASTER_ISSUE_MATRIX` flow back into Libraries via `CLAUDE_CODE_PROMPT` execution

---

## 10. `Libraries2.md` Deep Dive

**File**: `/home/sikmindz/Coding/Libraries2/Libraries2.md` (43 KB, 862 lines).

**Purpose**: a single-document comprehensive ecosystem snapshot — generated 2026-04-04, three days after the hostile audit.

**Structure** (19 sections):

| Section | Content |
|---|---|
| Workspace Overview | Members/excluded, shared dependencies |
| `stack-ids` | 217 ID types, `TraceCtx`, `ContentDigest`, tests |
| `job-queue` | ~3.5 k LOC; atomic claims, `QueueManager`, `QueueJob`, DB schema |
| `Tauri-Queue` | ~1.5 k LOC; `TauriEventEmitter`, `CoalescingEmitter`, event bridge |
| `AI-Batch-Queue` | ~1.8 k LOC; model-aware batching, ETA, resource-swap minimization |
| `LLM-Pipeline` | ~9.1 k LOC; `Payload` trait, `ExecCtx`, backends, output strategies, tool loops |
| `agent-graph` | ~4.7 k LOC; DAG execution, `AgentState`, routing, checkpointing, subgraphs |
| `ComfyUI-RS` | ~1.7 k LOC; `ComfyClient`, `Txt2ImgRequest`, WebSocket progress, workflow builder |
| `Ollama-Vision-RS` | ~671 LOC; tagging/captioning, 7-strategy parser, robustness |
| `llm-output-parser` | ~3.4 k LOC; JSON/YAML/XML/list/number parsing, repair, stripping |
| `Tauri-React-Hooks` | 518 LOC TypeScript; 6 hooks, async-safe cleanup, buffered streams |
| `constraint-compiler` | ~1.3 k LOC; projection-to-inference graphs, blake3 hashing |
| `discovery-portfolio` | 415 LOC; experiment discovery portfolios |
| `spec-execution` | 696 LOC; spec and proof surface types |
| `federated-settlement` | 625 LOC; cross-runtime treaty settlement |
| `attestation-exchange` | Minimal envelope contracts |
| `profile-runtime` | ~3.9 k LOC; constitution composition (non-functional, missing deps) |
| `remote-oracle-admission` | Minimal oracle validation |
| Non-Crate Directories | `overlay/`, `docs/`, `examples/`, `plans/`, `prompts/`, `schemas/` |

**Per crate**:
- Attributes table (version, MSRV, LOC, dependencies, features)
- Purpose paragraph
- Public API subsection (all public types, traits, functions, methods — signatures, not just names)
- Enums and Results
- Tests and examples count

**Notable depth examples**:
- `stack-ids` — lists all 217 ID newtypes by domain (core identity, kernel, control plane, governance, profiles)
- `job-queue` — full database schema, claim semantics, `FailureClass` patterns
- `LLM-Pipeline` — `OutputStrategy` enum variants, `Backend` trait implementations, `RetryConfig`, `ToolLoopRunner`
- `agent-graph` — node types, routing, reducers, checkpoint stores, macros

This is the **single source of truth for the ecosystem structure as of 2026-04-04**. Engineers or tooling can use it to understand "what exists" without reading 40 `Cargo.toml` files or 15 `lib.rs` modules.

---

## Summary

| Aspect | Finding | Status |
|---|---|---|
| Identity | Staging/development workspace for Libraries ecosystem; active audit + planning phase | Frozen snapshot (no git) |
| Workspace | 10 members, 6 excluded (Tauri/missing deps), 2 symlinks to external | Well-organized by domain |
| Governance | `CLAUDE.md`, `CLAUDE_CODE_PROMPT.md` (5 phases), `MASTER_ISSUE_MATRIX.md` (45 issues), `HOSTILE_AUDIT_REPORT.md` (7.8/10) | Latest 2026-03-26; actionable |
| Build state | 10 core crates compile; 6 excluded; PATH dep chaos blocking unified workspace | BLOCKING — PATH-1, PATH-2 critical |
| Git | Not a git repository; pure filesystem snapshot with backups | No version control |
| Audit findings | 2 CRITICAL, 8 HIGH, 22 MEDIUM, 13 LOW; `job-queue` 8.5/10, `agent-graph` 7.5/10, `Tauri-React-Hooks` zero tests | Comprehensive, phase-ordered fix plan |
| Symlinks | `llm-tool-runtime` → Libraries; `.parser-lib` → Gloss vendor | External dependencies, not local |
| Relationship | One-way snapshot from Libraries; staging area for `CLAUDE_CODE_PROMPT` refactoring; not a fork | Planning stage; fixes will backport to Libraries |
| Documentation | `Libraries2.md` (43 KB comprehensive catalog), plus `overlay/` governance, 3 backups (latest 2026-03-26) | Complete and detailed |

**Conclusion**: Libraries2 is a frozen governance and planning workspace capturing the state of the Utility Libraries Ecosystem at the critical juncture of a comprehensive hostile audit (2026-03-25). It serves as (1) an archive of audit findings and ecosystem state, (2) a planning surface for the 5-phase refactoring roadmap, and (3) a reference document via the comprehensive `Libraries2.md` snapshot.

It is **not a parallel development environment**; Recall and active work continue against the canonical `/home/sikmindz/Coding/Libraries`. The next phase of work is to reconcile Libraries2 findings back into Libraries via the `CLAUDE_CODE_PROMPT` execution plan.
