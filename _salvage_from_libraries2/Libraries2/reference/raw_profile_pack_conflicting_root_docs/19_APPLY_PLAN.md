
# Apply plan

## Apply order
1. extract this overlay at repo root
2. add root docs and the no-v25 rule
3. add `stack-ids` profile IDs
4. add owner-crate profile modules
5. register profile schemas in `contract-schema-gen`
6. add examples, manifests, fixtures, and conformance notes
7. update release-bar and curriculum truth docs
8. run pack truth checks
9. only then begin deeper behavior implementation

## First commits to make
- commit 1: docs + exec plan + release bar
- commit 2: `stack-ids` + owner module scaffolds
- commit 3: schemas + examples + manifests
- commit 4: fixtures + conformance
- commit 5: documentation truth + final checks
