# Master Issue Matrix — Current State V2
This matrix is built from static inspection of the `now.zip` snapshot. It is designed for Claude Code execution planning, not for marketing the stack.
## Method
- Repository snapshot unpacked and inspected file-by-file at the source/test/example level for the core crates.
- No workspace build or test run was possible in this environment, so runtime claims are classified by proof status.
- `semantic-memory-forge` was **not present** in this snapshot; end-to-end forge/evidence closure could not be re-audited from the archive.
## Summary
- Total actionable issues: **42**
- Mandatory next pass (`next_pass_mandatory=yes`): **26**
- P0 issues: **11**; P1: **13**; P2: **8**; P3: **10**

### By issue class
- **audit gap**: 1
- **phase-accepted compat debt**: 12
- **actual bug / target-state nonconformance**: 1
- **implementation lag**: 15
- **target-state nonconformance / implementation gap**: 1
- **implementation lag in tests**: 1
- **implementation lag / contract hardening**: 1
- **actual invariant gap**: 2
- **implementation lag / drift risk**: 2
- **implementation lag in test strategy**: 1
- **implementation lag / trace gap**: 1
- **phase-accepted lag coverage**: 1
- **actual lineage gap**: 1
- **phase-accepted compat debt / API ergonomics risk**: 1
- **phase-accepted compat debt in tests**: 1

### By crate
- **AI-Batch-Queue**: 1
- **LLM-Pipeline**: 3
- **Tauri-Queue**: 2
- **agent-graph**: 4
- **forge-memory-bridge**: 4
- **job-queue**: 4
- **knowledge-runtime**: 9
- **semantic-memory**: 12
- **snapshot**: 1
- **stack-ids**: 2

## Highest-pressure items
- **I004** `forge-memory-bridge` — Claim supersession lineage is currently synthesized with a fresh ClaimVersionId whenever supersedes_claim_id is present. That invents a version ID instead of resolving the actual superseded version lineage.
- **I009** `semantic-memory` — import_projection_batch still normalizes malformed records into plausible-looking rows via unwrap_or_default()/default state values for IDs, claim_state, projection_family, subject_entity_id, predicate, recorded_at, alias IDs, evidence handles, and episode IDs.
- **I014** `semantic-memory` — claim_versions has only a non-unique index on (claim_id, preferred_open) WHERE preferred_open = 1, so the database does not enforce the single-preferred-open invariant.
- **I015** `semantic-memory` — relation_versions likewise lacks a hard uniqueness guarantee for preferred_open rows, leaving relation version truth-order semantics unenforced at the DB layer.
- **I020** `knowledge-runtime` — query() always generates a fresh TraceCtx and does not accept caller-supplied trace context, which breaks end-to-end trace continuity for upstream orchestrators.
- **I021** `knowledge-runtime` — The adapter only pushes namespace upstream. Domain/workspace_id/repo_id remain runtime-side warnings rather than real upstream enforcement.
- **I022** `knowledge-runtime` — Temporal execution is still best-effort post-filtering over hybrid search; when results lack temporal metadata the runtime degrades to hybrid and only emits a warning.
- **I031** `agent-graph` — Internal retry helpers reuse one AttemptId and one TrialId across the whole node execution and do not emit per-retry trial lineage. That collapses distinct concrete retries into one trial, which breaks the intended attempt/trial semantics.
- **I033** `job-queue` — Public queue events and JobContext still expose legacy trace_id and attempt_count fields. They are documented as migration-only but still form a broad compatibility surface.
- **I040** `LLM-Pipeline` — Deprecated crate-local TraceId remains public, which keeps a legacy trace contract alive in a core control-flow crate.

## Full matrix
|ID|Crate|File / Symbol|Class|Phase OK now?|V5 conformant now?|Mandatory next pass?|Fossilization risk|Proof status|Severity|Priority|Issue|Required fix|Acceptance test|
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
|I001|snapshot|`workspace inventory` / `semantic-memory-forge crate`|audit gap|no|unknown|yes|high|confirmed from snapshot contents|high|P0|Expected forge/evidence crate is absent from this snapshot, so end-to-end causal/evidence path cannot be audited or advanced from the archive alone.|If the crate still exists in the real workspace, include it in the next archive and Claude pass. If it was intentionally removed, update docs/specs and close all references to the missing layer.|Snapshot used for the next pass includes the forge/evidence crate or the architecture docs explicitly remove it without dangling references.|
|I002|stack-ids|`stack-ids/src/trace.rs` / `TraceCtx legacy helpers`|phase-accepted compat debt|yes|mostly|no|medium|confirmed by code/docs|low|P3|Legacy trace compatibility constructors/helpers remain public, which is acceptable for migration but still leaves a public backward-compat surface in the canonical primitive crate.|Keep helper APIs but tighten docs/removal conditions; remove once downstream crates stop using legacy TraceId/string paths.|No downstream core crate requires legacy helper-only behavior; compat helpers can be behind a feature flag or removed without breakage.|
|I003|stack-ids|`stack-ids/src/scope.rs` / `ScopeKind::Compatibility`|phase-accepted compat debt|yes|mostly|no|low|confirmed by code/docs|low|P3|A compatibility-only scope kind remains in the primitive crate. Not wrong now, but it can fossilize if callers keep routing through it.|Leave for migration only; remove when no core crate emits or consumes compatibility scope kind.|Workspace search finds no runtime/core call sites using ScopeKind::Compatibility.|
|I004|forge-memory-bridge|`forge-memory-bridge/src/transform.rs` / `supersedes_claim_version_id mapping`|actual bug / target-state nonconformance|no|no|yes|high|confirmed by code path|critical|P0|Claim supersession lineage is currently synthesized with a fresh ClaimVersionId whenever supersedes_claim_id is present. That invents a version ID instead of resolving the actual superseded version lineage.|Do not mint a fake ClaimVersionId. Either carry a real superseded claim_version_id in the export schema, look it up via a real mapping layer, or leave the field None until real version lineage exists.|Supersession import tests prove that the bridge preserves a real prior claim_version_id or intentionally leaves it unset; no code path generates synthetic superseded version IDs from thin air.|
|I005|forge-memory-bridge|`forge-memory-bridge/src/batch.rs` / `ProjectionImportBatchV1.schema_version`|implementation lag|yes|not fully|yes|medium|confirmed by code/docs/tests|medium|P1|Import-batch version vocabulary is still a single generic schema_version field, which conflates export and import-side schema naming and will become confusing as soon as V2/Vn diverge.|Rename or split version fields so the bridge batch can distinguish export_schema_version from import_schema_version (or equivalent wording) without ambiguity.|Batch type, transform tests, and importer logs use distinct import/export version vocabulary; docs no longer say the field simply mirrors export version forever.|
|I006|forge-memory-bridge|`forge-memory-bridge/src/batch.rs; forge-memory-bridge/src/transform.rs` / `recorded_at semantics`|implementation lag|yes|partially|yes|medium|confirmed by code/tests|medium|P2|recorded_at is bridge-stamped at transform time, but comments across the stack have historically described it as importer-owned transaction time. The code is now consistent internally, but the semantic contract still needs one unambiguous owner/meaning.|Choose one meaning and align code/docs. If recorded_at is bridge-stamped transform time, say so everywhere and use a different field for importer transaction time if needed.|Bridge docs, batch docs, semantic-memory importer docs, and tests all describe the same meaning for recorded_at/transformed_at/imported_at.|
|I007|forge-memory-bridge|`forge-memory-bridge/src/legacy.rs` / `LegacyImportEnvelopeV1 shim`|phase-accepted compat debt|yes|not for final release|no|medium|confirmed by code/docs|low|P3|Legacy compatibility shim remains public. Acceptable for migration, but still a fossilization risk if callers never leave it.|Keep phase labels/removal condition explicit; avoid adding any new features to the shim; remove once all callers use ExportEnvelopeV1 -> transform_envelope().|Workspace search shows no new call sites on legacy shim; removal can be scheduled after caller migration.|
|I008|semantic-memory|`semantic-memory/src/lib.rs` / `ImportBatchPayload / payload structs`|implementation lag|yes|not fully|yes|high|confirmed by code|high|P0|Local mirror payload structs mark many canonically-required fields as Option<_>, which makes the importer structurally tolerant of malformed projection rows before semantic validation runs.|Move required-field validation up to the import boundary. Keep local mirror structs if needed for dependency hygiene, but explicitly validate all canonically-required fields before building storage rows.|Malformed batches missing required claim/relation/alias/evidence fields fail fast with ImportInvalid instead of being default-filled.|
|I009|semantic-memory|`semantic-memory/src/lib.rs` / `import_projection_batch default-filling`|target-state nonconformance / implementation gap|no|no|yes|high|confirmed by code|critical|P0|import_projection_batch still normalizes malformed records into plausible-looking rows via unwrap_or_default()/default state values for IDs, claim_state, projection_family, subject_entity_id, predicate, recorded_at, alias IDs, evidence handles, and episode IDs.|Replace semantic default-filling with explicit validation errors for required canonical fields. Reserve defaults only for fields the spec explicitly marks optional.|Regression tests assert that missing claim_version_id/claim_id/content/subject/predicate/etc. are rejected, not silently normalized.|
|I010|semantic-memory|`semantic-memory/tests/import_ugly_cases.rs` / `claim_with_minimal_fields_imports`|implementation lag in tests|yes|no|yes|high|confirmed by test|high|P0|A test currently locks in permissive minimal-claim import behavior where only claim_version_id, claim_id, and content are supplied. That hardens the very default-filling behavior the importer should stop doing.|Replace the permissive test with stricter required-field validation tests that match the canonical logical contract.|Test suite fails when required canonical fields are omitted from claim/relation rows and passes only for schema-complete imports.|
|I011|semantic-memory|`semantic-memory/src/lib.rs` / `import_projection_batch(&str)`|implementation lag / contract hardening|yes|partially|yes|medium|confirmed by public API|medium|P1|The canonical import path still enters semantic-memory as a JSON string boundary. That is not automatically wrong, but it remains a drift-risk seam and pushes correctness onto local deserialization/validation rather than a stronger shared contract.|Keep dependency direction clean, but harden the seam with explicit contract fixtures/round-trip tests and stricter validation. Consider extracting a tiny shared import-boundary crate only if it preserves package law.|Bridge-produced batch fixtures deserialize/import losslessly in semantic-memory; incompatible batches fail with deterministic boundary errors.|
|I012|semantic-memory|`semantic-memory/src/lib.rs` / `trace_ctx handling in import_projection_batch`|implementation lag|yes|partially|yes|medium|confirmed by code|medium|P1|The importer reduces incoming trace_ctx to trace_id only, losing any richer canonical trace context that may matter for parent/span/baggage continuity.|Define and store a canonical trace reference model explicitly. If full TraceCtx is not stored, document the exact persisted reference and keep conversion loss bounded and intentional.|Imported projection/log rows preserve either full TraceCtx or an explicitly documented canonical trace reference shape; tests prove round-trip semantics.|
|I013|semantic-memory|`semantic-memory/src/projection_storage.rs` / `trace_id TEXT columns`|implementation lag|yes|partially|no|medium|confirmed by schema|medium|P2|Projection tables and import logs still persist only trace_id TEXT fields. This may be acceptable as a canonical trace reference, but the contract is not sharp enough and currently loses parent/baggage semantics.|Either bless trace_id TEXT as the intended canonical persisted reference everywhere or extend storage for richer trace reference data. Do not leave it ambiguous.|Schema/docs/tests agree on what trace data is durable and what is intentionally ephemeral.|
|I014|semantic-memory|`semantic-memory/src/projection_storage.rs` / `claim_versions preferred_open invariant`|actual invariant gap|no|no|yes|high|confirmed by schema|critical|P0|claim_versions has only a non-unique index on (claim_id, preferred_open) WHERE preferred_open = 1, so the database does not enforce the single-preferred-open invariant.|Add a partial UNIQUE constraint/index or equivalent import-time validation that guarantees at most one preferred_open row per logical claim key.|A batch that would create two preferred_open claim versions for the same claim fails atomically; happy-path tests still pass.|
|I015|semantic-memory|`semantic-memory/src/projection_storage.rs` / `relation_versions preferred_open invariant`|actual invariant gap|no|no|yes|high|confirmed by schema|high|P0|relation_versions likewise lacks a hard uniqueness guarantee for preferred_open rows, leaving relation version truth-order semantics unenforced at the DB layer.|Add an equivalent uniqueness rule for relation_versions preferred_open semantics or enforce it transactionally before insert.|Importer rejects batches that would create multiple preferred_open relation versions for the same logical relation key.|
|I016|semantic-memory|`semantic-memory/src/projection_import.rs` / `ImportEnvelope legacy path`|phase-accepted compat debt|yes|not for final release|no|medium|confirmed by code/docs/tests|medium|P3|Legacy ImportEnvelope/ImportRecord path remains public and heavily tested. Acceptable for migration, but still a prominent alternate ingestion surface.|Do not add new features to the legacy path. Keep phase labels explicit and continue migrating all new work to import_projection_batch.|No new code or tests are added for legacy path except migration coverage; canonical path coverage becomes dominant.|
|I017|semantic-memory|`semantic-memory/src/lib.rs; knowledge-runtime/tests/cross_crate_proof.rs` / `local mirror boundary contract`|implementation lag / drift risk|yes|partially|yes|high|high-confidence inference from current structure|medium|P1|semantic-memory intentionally mirrors bridge batch types locally, but the workspace still lacks a dedicated contract harness proving the local mirror stays wire-compatible as fields evolve.|Add bridge->memory contract fixtures/round-trip tests that serialize real bridge batches and import them via semantic-memory without lossy/default-filled behavior.|Cross-crate contract tests fail if bridge and semantic-memory payload schemas drift.|
|I018|semantic-memory|`semantic-memory/tests/import_boundary_tests.rs` / `legacy-only boundary coverage`|implementation lag in test strategy|yes|partially|yes|medium|confirmed by test file|medium|P2|The dedicated “import boundary” test suite still focuses on the legacy ImportEnvelope path instead of the canonical projection-batch boundary. Canonical path has tests elsewhere, but the split still biases coverage toward the old seam.|Create/expand a dedicated canonical batch boundary test suite and demote legacy-boundary tests to compatibility coverage.|There is a first-class canonical import boundary suite that covers schema validation, idempotency, rollback, invariants, and trace/provenance preservation.|
|I019|semantic-memory|`semantic-memory/src/projection_storage.rs` / `projection_import_log.schema_version`|implementation lag|yes|not fully|no|medium|confirmed by schema|low|P2|projection_import_log still uses generic schema_version naming, mirroring the bridge vocabulary ambiguity and making future export/import divergence harder to reason about.|Align log vocabulary with the bridge import/export version decision so batch logs stay unambiguous.|Import logs and docs use the same distinct version vocabulary as the bridge batch.|
|I020|knowledge-runtime|`knowledge-runtime/src/runtime.rs` / `KnowledgeRuntime::query trace entry`|implementation lag / trace gap|yes|no|yes|high|confirmed by API/code|high|P0|query() always generates a fresh TraceCtx and does not accept caller-supplied trace context, which breaks end-to-end trace continuity for upstream orchestrators.|Add an API path that accepts inbound TraceCtx (or a request object carrying it) and threads it through QueryTrace instead of always generating a fresh root.|Runtime query traces can preserve an upstream trace ID/parent span when provided; existing convenience API may still auto-generate when none is supplied.|
|I021|knowledge-runtime|`knowledge-runtime/src/adapters/semantic_memory.rs` / `SemanticMemoryAdapter::search`|implementation lag|yes|no|yes|high|confirmed by code/docs|high|P0|The adapter only pushes namespace upstream. Domain/workspace_id/repo_id remain runtime-side warnings rather than real upstream enforcement.|Add richer scope pushdown once semantic-memory supports it, or add an explicit scope-filtering stage over returned results that is treated as first-class execution rather than only a warning.|Queries with extra scope dimensions are either fully enforced or fail/emit a stronger “not supported” path; no silent widening.|
|I022|knowledge-runtime|`knowledge-runtime/src/runtime.rs` / `temporal route execution`|implementation lag|yes|no|yes|high|confirmed by code/docs/tests|high|P0|Temporal execution is still best-effort post-filtering over hybrid search; when results lack temporal metadata the runtime degrades to hybrid and only emits a warning.|Implement true temporal retrieval semantics over imported temporal metadata or a dedicated temporal index/view, rather than only post-filtering opaque SearchResult objects.|Temporal queries return time-scoped results without downgrade on data that contains valid_from/valid_to; degradation remains explicit only for genuinely unsupported paths.|
|I023|knowledge-runtime|`knowledge-runtime/src/runtime.rs; knowledge-runtime/src/config.rs` / `projection.persist`|implementation lag|yes|no|yes|medium|confirmed by code/docs|medium|P1|projection.persist is accepted in config but ignored at runtime; projections remain in-memory only.|Either implement persistence/rebuild support or reject the flag instead of accepting-and-ignoring it.|Setting projection.persist either persists/reloads projections correctly or returns a configuration error stating the feature is unsupported.|
|I024|knowledge-runtime|`knowledge-runtime/src/runtime.rs` / `projection rebuild execution`|implementation lag|yes|no|yes|medium|confirmed by docs/API|medium|P1|Projection tracker records invalidation/build/failure state, but the runtime still does not execute rebuilds itself.|Add explicit rebuild orchestration hooks/jobs or demote the tracker to pure observability until real rebuild execution exists.|A projection can be invalidated and then rebuilt through a runtime-owned API/job path with traceable lifecycle transitions.|
|I025|knowledge-runtime|`knowledge-runtime/src/lib.rs; knowledge-runtime/src/runtime.rs` / `Forge causal adapter`|implementation lag|yes|no|no|medium|confirmed by docs|medium|P2|The runtime still has no Forge/causal projection adapter, so memory-visible causal projections cannot participate in real query-time orchestration yet.|Add a memory-visible causal projection consumption path only after the forge/evidence crate is back in scope and the import contract is stable.|Runtime can consume imported causal projections through the same memory-visible boundary as other records, without joining raw Forge truth directly.|
|I026|knowledge-runtime|`knowledge-runtime/src/lib.rs; knowledge-runtime/src/entity/registry.rs` / `entity resolution`|implementation lag|yes|no|no|medium|confirmed by docs/code|medium|P2|Entity resolution is still exact canonical/exact alias only; no fuzzy resolution, candidate expansion, or confidence-ranked matching exists yet.|Add configured fuzzy resolution/candidate expansion against canonical memory state once scope/temporal basics are stable.|Entity queries can return ranked exact + fuzzy candidates with explicit quality flags and no mutation of authoritative identity state.|
|I027|knowledge-runtime|`knowledge-runtime/src/runtime.rs` / `entity_registry_mut`|phase-accepted compat debt|yes|not for final release|yes|high|confirmed by API|medium|P1|entity_registry_mut() is deprecated but still publicly callable, so authority-like mutation of the runtime cache is still possible via the old API.|Remove or gate the deprecated mutator after migrating call sites to refresh_entity_cache(); keep only fenced cache-refresh operations.|No core crate or tests rely on entity_registry_mut(); deprecation can be turned into removal without loss of required functionality.|
|I028|knowledge-runtime|`knowledge-runtime/tests/cross_crate_proof.rs; knowledge-runtime/tests/ugly_case_tests.rs` / `degraded behavior assertions`|phase-accepted lag coverage|yes|partially|no|medium|confirmed by tests|low|P3|Current tests intentionally prove downgrade/warning behavior for temporal and scope gaps. That is good now, but those tests will fossilize lag unless they are revised when the features land.|Keep these until real temporal/scope support lands, then replace them with stricter execution assertions.|Once runtime support lands, downgrade-only tests are removed or inverted into success-path tests.|
|I029|agent-graph|`agent-graph/src/event_sink.rs` / `GraphEvent legacy fields`|phase-accepted compat debt|yes|not for final release|yes|high|confirmed by public API/docs|high|P1|Graph events still carry public legacy trace_id: String and attempt: u32 fields on the normal event surface. They are phase-labeled, but they remain easy for callers to fossilize against.|Keep canonical trace_ctx/attempt_id/trial_id primary in docs/examples now, then remove legacy fields after caller migration.|No core consumer reads legacy event fields exclusively; canonical fields are present and used in all maintained examples/tests.|
|I030|agent-graph|`agent-graph/src/graph.rs` / `legacy_trace_id() event emission`|phase-accepted compat debt|yes|not fully|yes|high|confirmed by code|medium|P1|The graph executor still derives and emits legacy trace strings at many event and payload sites, so canonical TraceCtx is present but not yet the uncontested operational normal path.|Route internal logic and event emission through canonical trace_ctx first, deriving trace_id only at explicit compatibility boundaries.|Workspace search shows legacy_trace_id() use only at clearly marked compatibility boundaries, not throughout graph execution.|
|I031|agent-graph|`agent-graph/src/graph.rs` / `execute_with_retry_* / NodeStart / NodeEnd lineage`|actual lineage gap|no|no|yes|high|confirmed by code path|critical|P0|Internal retry helpers reuse one AttemptId and one TrialId across the whole node execution and do not emit per-retry trial lineage. That collapses distinct concrete retries into one trial, which breaks the intended attempt/trial semantics.|Mint one AttemptId per retry family and a fresh TrialId for each concrete internal retry attempt. Emit retry-aware events/checkpoint metadata per trial.|A node with retry policy produces one stable AttemptId and multiple TrialIds across internal retries; tests verify per-trial event/checkpoint lineage.|
|I032|agent-graph|`agent-graph/src/graph.rs` / `attempt field on events/checkpoints`|implementation lag|yes|no|yes|medium|confirmed by code|medium|P1|NodeStart/checkpoint recording currently uses attempt: 0 even when retry policy may perform additional internal attempts. The legacy attempt counter therefore does not reflect real retry count.|Track and emit actual retry-attempt count whenever internal retry is used, or remove the legacy counter from normal-path semantics faster.|Retry tests show attempt counter increments consistently with per-trial lineage.|
|I033|job-queue|`job-queue/src/events.rs; job-queue/src/lib.rs` / `trace_id / attempt_count public fields`|phase-accepted compat debt|yes|not for final release|yes|high|confirmed by public API/docs|high|P1|Public queue events and JobContext still expose legacy trace_id and attempt_count fields. They are documented as migration-only but still form a broad compatibility surface.|Continue migration toward canonical trace_ctx/attempt_id/trial_id, then remove legacy event/context fields once consumers migrate.|All maintained consumers/examples can operate on canonical fields only; legacy fields can be feature-gated or removed.|
|I034|job-queue|`job-queue/src/db.rs` / `queue_jobs durable trace storage`|implementation lag|yes|partially|no|medium|confirmed by schema/code|medium|P2|Durable storage still keeps only trace_id TEXT rather than a richer canonical trace reference/TraceCtx representation. Canonical context is reconstructed on load.|Decide the durable canonical trace reference shape explicitly. Either keep trace_id as the blessed persisted reference or extend the schema for richer trace metadata.|DB schema/docs/tests all agree on what trace information survives queue persistence and replay.|
|I035|job-queue|`job-queue/src/executor.rs` / `trace_ctx reconstruction`|implementation lag|yes|partially|no|medium|confirmed by code|medium|P2|Executor reconstructs canonical TraceCtx from persisted trace_id for jobs loaded from DB, which loses parent span/baggage continuity when only the legacy string survives.|If richer trace continuity matters for queued work, persist an explicit trace reference structure instead of reconstructing solely from trace_id.|Replay/queue-hop tests prove the intended level of trace continuity (trace only vs trace+parent+baggage) and match the schema/docs.|
|I036|job-queue|`job-queue/src/lib.rs` / `JobContext::new_direct schema bootstrap`|implementation lag / drift risk|yes|partially|no|medium|confirmed by code|low|P3|new_direct() bootstraps a minimal in-memory queue_jobs table that does not mirror the current canonical queue schema, which can let the direct-execution path drift from real queue semantics.|Either align the direct schema with current DB shape or clearly document that new_direct() is a lightweight non-persistent convenience path with reduced lineage semantics.|Direct execution path either uses schema-compatible setup or is explicitly documented/tested as intentionally reduced functionality.|
|I037|AI-Batch-Queue|`AI-Batch-Queue/src/lib.rs` / `build_job()`|phase-accepted compat debt / API ergonomics risk|yes|partially|no|medium|confirmed by API/docs|low|P3|The default build_job() helper initializes trace_ctx/attempt_id/trial_id to None. That is convenient, but it makes it easy for callers to enqueue untraced batch items unless they consciously choose build_job_traced().|Keep build_job() for convenience, but make traced construction more prominent in docs/examples or add a queue-level stamping hook so untraced jobs are less likely in production paths.|Primary examples and docs use build_job_traced() or otherwise stamp canonical lineage before execution.|
|I038|Tauri-Queue|`Tauri-Queue/src/lib.rs` / `EmitterConfig include_trace_id / should_keep_trace`|phase-accepted compat debt|yes|not for final release|no|medium|confirmed by public API/docs|medium|P3|The Tauri bridge still carries a dual include_trace_id/include_trace_ctx configuration model that keeps legacy string-first semantics alive in the UI layer.|Retain until UI consumers migrate, but converge on TraceCtx-first event handling and eventually remove include_trace_id.|Frontend integration tests consume canonical trace_ctx (or its explicit serialized replacement) without requiring include_trace_id.|
|I039|Tauri-Queue|`Tauri-Queue/tests/integration_tests.rs` / `legacy trace compat cases`|phase-accepted compat debt in tests|yes|partially|no|medium|confirmed by tests|low|P3|Integration tests still deliberately validate legacy trace-string compatibility behavior. Useful now, but they will fossilize the old surface if they outlive consumer migration.|Keep until migration is done, then trim the legacy matrix and keep only canonical event cases.|Once UI migration lands, legacy-only test cases are deleted or moved behind a compat feature.|
|I040|LLM-Pipeline|`LLM-Pipeline/src/trace.rs` / `TraceId`|phase-accepted compat debt|yes|not for final release|yes|high|confirmed by public API/docs|high|P1|Deprecated crate-local TraceId remains public, which keeps a legacy trace contract alive in a core control-flow crate.|Continue migration to stack_ids::TraceCtx and remove TraceId once remaining callers are gone. Avoid adding any new APIs that require TraceId.|No maintained examples/core crates depend on TraceId; deprecation can be escalated to removal.|
|I041|LLM-Pipeline|`LLM-Pipeline/src/exec_ctx.rs` / `ExecCtx / ExecCtxBuilder trace fields`|phase-accepted compat debt|yes|not fully|yes|high|confirmed by code/docs|medium|P1|ExecCtx and its builder still carry both trace_id and trace_ctx, and build() always derives/retains the legacy TraceId. The crate is still dual-world rather than trace-native.|Keep canonical TraceCtx primary and push legacy TraceId behind explicit compatibility boundaries; eventually remove trace_id from ExecCtx once callers migrate.|Normal-path builder usage can be fully canonical without touching TraceId; legacy path is isolated and removable.|
|I042|LLM-Pipeline|`LLM-Pipeline/src/lib.rs` / `compatibility re-exports`|phase-accepted compat debt|yes|partially|no|medium|confirmed by docs|low|P3|The crate still foregrounds broad compatibility re-exports, which can keep older API shapes feeling normative longer than intended.|Make canonical entrypoints more prominent in crate docs/examples and trim compatibility re-exports after migration.|Top-level docs/examples lead with canonical APIs rather than compatibility ones.|
