Perform a hostile-but-constructive v10 finishline pass.

Focus on:
- where graph roles are still blended
- where region boundaries are implicit
- where recomputation still falls back to blind rebuild
- where contradictions are still handled narratively instead of via repair surfaces
- where nuisance state is still metadata instead of modeled input
- where iterative operators can oscillate silently
- where benchmark claims are not yet provable

Output:
- concrete code/doc/test changes
- proof obligations
- benchmark fixture ideas
- no new doctrine sprawl
