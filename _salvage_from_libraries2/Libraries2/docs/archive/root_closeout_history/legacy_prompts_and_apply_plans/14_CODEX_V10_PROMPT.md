Implement the v10 finishline against the current March 13 snapshot.

Assume v9 is effectively closed enough to support v10 execution work.

Priorities:
1. explicit graph geometry separation
2. bounded regions as the default unit of work
3. time-aware incremental recomputation
4. syndrome-first repair routing
5. nuisance-state artifacts
6. convergence governance
7. oracle slices and parity checks
8. runnable benchmark proof

Rules:
- do not create another giant root-doc stratum
- do not collapse graph roles into one structure
- do not let global rebuild become the quiet default
- every implementation step should yield a proof artifact, fixture, or schema
