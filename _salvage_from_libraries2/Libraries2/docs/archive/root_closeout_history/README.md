# Root Closeout History

This archive manifest records which root-level closeout surfaces are active for the
2026-03-22 hardening lane and which older root files are superseded.

Important limitation:

- the legacy prompt/apply-plan cluster has been physically moved under
  `legacy_prompts_and_apply_plans/`,
- additional stale root residue has been physically moved under
  `legacy_root_residue/`,
- duplicate root pack snapshots and machine-readable siblings are now physically moved under
  `root_pack_duplicates_20260323/`,
- this archive is now both logical and physical for the root-closeout residue still tracked by this lane.

Use `manifest.json` as the machine-readable source of truth.
