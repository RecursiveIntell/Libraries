# Codex Finish Operating Prompt

You are closing out the current bounded horizon pass for this Rust workspace.

## Prime directive

Do not widen the horizon.
Finish the unfinished seams already implied by the repo.

## What the repo already has

The repo already contains owner crates for:
- federation
- mechanism/theory
- discovery portfolio
- constitutional memory
- spec execution

It already has:
- schemas,
- fixtures,
- manifests,
- and some reference-interpreter coverage.

Your job is to turn that into a **truthful closeout pass**.

## What to do

1. Add the missing high-value artifact families.
2. Deepen the current evaluators one notch.
3. Add fixtures and tests for those richer slices.
4. Keep `contract-schema-gen`, manifests, and tests in sync.
5. Add crate-local README/AGENTS files for the new owner crates.
6. Update repo-level docs so the code and docs agree.

## What not to do

- Do not claim full v16–v20 completion.
- Do not create a new truth plane.
- Do not silently promote advisory artifacts.
- Do not add giant speculative orchestration layers.
- Do not refactor broadly just to make the diff feel impressive.

## Special mandatory closure

You must add `SelfHostingBuildReceiptV1` end-to-end.
The spec build order names it, and the current repo does not yet own it.

## Implementation posture

Prefer:
- small lawful additions,
- explicit IDs,
- schema-first artifact ownership,
- fixture-backed tests,
- bounded reference interpreters,
- honest advisory/degraded states.

Avoid:
- placeholder types with no tests,
- new artifacts with no schema owner,
- duplicated semantics across crates,
- or wide cross-crate coupling.

## Definition of success

A smaller truthful pass is better than a larger fake one.
If you cannot make a surface real enough, leave it clearly bounded and explicitly non-admitted.
