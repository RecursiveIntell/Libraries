# Codex operating prompt — v25 production closure

- Start from the generated gap audit, not from memory.
- First close `effect-runtime`; it is the only target still on raw `String` IDs.
- Use `stack-ids` citations in policy/control/adjudication to avoid dependency cycles.
- Consumers may cite compiled obligations; they may not recompute raw profile interaction.
- Every new external field requires schema/example/test updates in the same pass.
- Do not wire the strict final gate into CI until the code and examples actually satisfy it.
- Re-run the audit and final command list before claiming closure.
