# v21/v24 reference fixtures

Minimum executable fixture lanes to add:

- one effect happy path and one blocked preflight path,
- one delegated effect path and one revoked lease path,
- one release-ready deployment and one blocked-assurance path,
- one incident -> containment -> recovery -> postmortem path,
- one continuity exception with mandatory post-hoc review.
