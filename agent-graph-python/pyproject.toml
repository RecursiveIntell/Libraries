[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "agent-graph"
version = "0.1.0"
description = "Python bindings for agent-graph"
requires-python = ">=3.9"

[tool.maturin]
module-name = "agent_graph._native"
python-source = "python"
