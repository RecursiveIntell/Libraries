# 00 START HERE

## Is this worth doing?

Yes — very, but only if the integration starts as optional/shadow/evidence-backed.

Expected value:

- immediate: a real evaluation harness for turbo-quant using semantic-memory workloads;
- near-term: optional compressed vector sidecar with approximate scoring;
- long-term: compressed search/cold-tier memory and possible compressed ANN path;
- stack-level: codec profiles, receipts, and approximation disclosure aligned with provenance-first design.

Do **not** start by making TurboQuant the default vector store.

## Run posture

Paste:

1. `MANUAL_INJECTIONS/INJECTION_00_PRESTART.md`
2. `MAIN_CODEX_PROMPT.md`

Then feed phase prompts in order, with manual injections at the indicated gates.

## Key precondition

A clean Cargo dependency needs `turbo-quant` as a relative dependency. Preferred:

```text
/home/sikmindz/Coding/Libraries/turbo-quant
/home/sikmindz/Coding/Libraries/semantic-memory
```

If this is not true, Codex must report it instead of inventing a brittle workaround.
