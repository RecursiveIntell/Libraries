# 09 — Phase 0 Prompt

Use this prompt for a smaller Codex pass if the full `PROMPT.md` is too broad.

> You are in `~/Coding/Libraries/AiDENs`. Implement Phase 0 only. Build `aidens-contracts`, `aidens-boundary-kit`, `aidens-config`, `aidens-receipts`, `aidens-capability-kit`, and `aidens-testkit`. Do not inspect or modify app shells yet. Use `~/Coding/Recall` only for source lessons. Add tests for config redaction, strict JSON parsing, boundary repair receipt, receipt ledger append behavior, capability truth states, and schema derivation. Run cargo fmt/test if possible. Do not import Tauri/daemon/queue/memory crates into Phase 0 crates.
