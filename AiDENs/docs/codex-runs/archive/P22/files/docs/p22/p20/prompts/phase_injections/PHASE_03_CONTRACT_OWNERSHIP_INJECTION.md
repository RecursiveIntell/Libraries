Proceed to Phase 03 only.

Focus: `aidens-contracts` and local type ownership.

For every public type in `crates/aidens-contracts/src/lib.rs`, classify:

- canonical_reexport;
- aidens_orchestration_dto;
- display_or_report_projection;
- compatibility_legacy_adapter;
- duplicate_canonical_concept;
- ambiguous_shadow_semantics.

Delete, redirect, rename, or quarantine every duplicate/ambiguous canonical concept.

Forbidden local ownership:

- episode identity law;
- claim/evidence truth;
- bitemporal truth;
- kernel/witness/syndrome truth;
- verification/control truth;
- repair/contradiction law.

Create inventory `.md` and `.json`.

Stop after Phase 03.
