# Explicit Non-Goals

This run must not attempt:

- splitting `aidens-contracts`;
- provider implementation;
- UI polish;
- daemon/scheduler/queue feature work;
- new kernel algorithms;
- new federation/mechanism features;
- Recall/Recall-Coding porting;
- performance optimization;
- new application behavior.

The only acceptable code changes are those necessary to collapse canonical ownership violations, wire canonical owners, install gates, update docs, and preserve compile/test correctness.
