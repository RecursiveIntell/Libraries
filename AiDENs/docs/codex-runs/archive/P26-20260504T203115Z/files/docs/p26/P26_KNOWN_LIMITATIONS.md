# P26 Known Limitations

- P26 is not production-cloud-ready.
- Cloud provider execution remains deferred.
- Broad autonomous daemon behavior remains deferred.
- V10+ runtime geometry remains design-only.
- Memory-grounded agent evidence uses canonical adapter routes; AiDENs does not own memory truth.
- Repair and abstention records are local display/operator artifacts, not canonical repair truth.
- Full workspace test, clippy, doc, strict package validation, and package self-replay passed in Phase 09/final evidence.
- The local coding example intentionally abstains without scoped permits for write/check actions.
- Memory-grounded agents remain partial because AiDENs records canonical-seam evidence but does not own memory truth.
- `/tmp` was capacity-constrained during self-replay; final replay evidence uses `TMPDIR=target/p26/tmp`.
- `assert_package_self_replay.py` still uses a historical `p23_replay_` temporary prefix; this is stale cosmetic naming, not behavior failure.
