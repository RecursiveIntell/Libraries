# 11 — Phase 2 Prompt

> You are in `~/Coding/Libraries/AiDENs`. Implement the app speed layer: `aidens-app-kit`, `aidens`, and `aidens-cli`. Add `AiDENsApp::builder`, profile expansion to `AiDENsAppPlanV1`, `aidens doctor`, `aidens check-config`, `aidens list-tools`, `aidens list-capabilities`, and a minimal generated app template. The generated app must be safe-by-default and include tests. Dangerous tools must require explicit profile plan changes and permits.
