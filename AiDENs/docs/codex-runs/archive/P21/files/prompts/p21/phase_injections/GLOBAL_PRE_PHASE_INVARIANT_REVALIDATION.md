You are entering a new P21 phase.

Before doing any work, revalidate the following invariants against the actual code:

1. AiDENs is an orchestration/profile/policy/product layer, not a replacement for canonical stack crates.
2. No shadow truth: no local memory/evidence/kernel/repair/verification/federation/mechanism truth.
3. Use `stack-ids` from `libraries`, not `libraries2`.
4. No silent semantic widening, fallback, compatibility shim, or parser repair without explicit receipt/degradation record.
5. Execution is evidence: provider route, tool calls, permits, retries, budget exhaustion, queue hops, degraded paths, and failures must be receipt-bearing.
6. Agency/influence policy must be a runtime gate when enabled, not prompt-only language.
7. Provider/tool capability truth must be executable and tested. Unsupported providers stay unavailable.
8. Generated agents must be runnable; examples that do not run are failures.
9. Do not delete tests, fixtures, evals, or scanners to make gates pass.
10. If ownership is unclear, stop and report/quarantine; do not invent local semantics.

At the start of the phase, explicitly state:

- which files/crates you will touch;
- which invariants are most at risk;
- which commands will prove the phase.

If any invariant is already violated, repair or stop before doing phase work.
