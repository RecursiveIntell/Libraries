# Phase 02 — Test-Agent CLI

Implement or repair `cargo run -p aidens-cli -- run-test-agent fixtures/test-agent/basic-agent.toml`.

It must use real `AiDENsRunner`, provider/tool/permit/boundary/agency/receipt path, and emit output artifacts.

Write `handoffs/p21/PHASE_02_TEST_AGENT_CLI.md`.
