# Phase 04 — Profile + Plan Kit Usability

Make profile/plan surfaces product-usable. `chat-only` and `coding-agent` should be supported; memory/daemon/research should be partial or deferred honestly.

Make plan compile/validate work for test-agent and generated-agent configs.
