--- .codex_evidence/contract_ownership/04/pre_edit_files/aidens_contracts_lib.rs.before	2026-04-29 04:02:18.173133234 -0500
+++ crates/aidens-contracts/src/lib.rs	2026-04-29 04:07:21.819548112 -0500
@@ -6,9 +6,8 @@
 use chrono::{DateTime, Duration, Utc};
 use schemars::JsonSchema;
 use serde::{Deserialize, Serialize};
-use sha2::{Digest, Sha256};
 use std::collections::{BTreeMap, BTreeSet};
-use std::fmt::{self, Write as _};
+use std::fmt;
 use uuid::Uuid;
 
 /// Canonical identity and digest surfaces from the shared stack.
@@ -42,6 +41,7 @@
     }
 }
 
+pub use attestation_exchange::AttestationEnvelopeV1;
 pub use canonical_stack::CanonicalVerificationDisposition;
 pub use canonical_stack::{
     ArtifactId, ArtifactId as StackArtifactId, AttemptId as StackAttemptId,
@@ -52,7 +52,6 @@
     KernelRunId as StackKernelRunId, ProjectionId as StackProjectionId, Scope as StackScope,
     ScopeKey as StackScopeKey, TraceCtx as StackTraceCtx, TrialId as StackTrialId,
 };
-pub use attestation_exchange::AttestationEnvelopeV1;
 pub use federated_settlement::{SettlementCaseV1, SharedDispositionV1};
 pub use mechanism_runtime::{HypothesisLibraryV1, TheoryRefuterSuiteV1, TheoryVersionV1};
 
@@ -61,59 +60,33 @@
 }
 
 fn json_digest(value: &serde_json::Value) -> String {
-    stable_json_digest(value)
+    non_authoritative_json_display_digest(value)
 }
 
-pub fn stable_json_digest(value: &serde_json::Value) -> String {
-    let normalized = canonical_json_string(value);
-    stable_text_digest(&normalized)
+pub fn non_authoritative_json_display_digest(value: &serde_json::Value) -> String {
+    let digest = canonical_stack::digest_json(value)
+        .expect("serde_json::Value serialization is infallible for stack digesting");
+    non_authoritative_display_digest_string(&digest)
 }
 
-pub fn stable_text_digest(text: &str) -> String {
-    let digest = Sha256::digest(text.as_bytes());
-    let mut encoded = String::with_capacity("sha256:".len() + 64);
-    encoded.push_str("sha256:");
-    for byte in digest {
-        let _ = write!(&mut encoded, "{byte:02x}");
-    }
-    encoded
+pub fn non_authoritative_text_display_digest(text: &str) -> String {
+    let digest = StackContentDigest::compute_str(text);
+    non_authoritative_display_digest_string(&digest)
 }
 
-pub fn deterministic_artifact_id(prefix: &str, material: &str) -> ArtifactId {
-    let digest = stable_text_digest(material);
-    ArtifactId(format!(
-        "{}:{}",
-        prefix,
-        digest.strip_prefix("sha256:").unwrap_or(&digest)
-    ))
-}
-
-pub fn canonical_json_string(value: &serde_json::Value) -> String {
-    let canonical = canonical_json_value(value);
-    serde_json::to_string(&canonical).unwrap_or_default()
-}
-
-fn canonical_json_value(value: &serde_json::Value) -> serde_json::Value {
-    match value {
-        serde_json::Value::Array(items) => {
-            serde_json::Value::Array(items.iter().map(canonical_json_value).collect())
-        }
-        serde_json::Value::Object(map) => {
-            let mut canonical = serde_json::Map::new();
-            let mut keys = map.keys().collect::<Vec<_>>();
-            keys.sort();
-            for key in keys {
-                if let Some(child) = map.get(key) {
-                    canonical.insert(key.clone(), canonical_json_value(child));
-                }
-            }
-            serde_json::Value::Object(canonical)
-        }
-        serde_json::Value::Null
-        | serde_json::Value::Bool(_)
-        | serde_json::Value::Number(_)
-        | serde_json::Value::String(_) => value.clone(),
-    }
+fn non_authoritative_display_digest_string(digest: &StackContentDigest) -> String {
+    format!("blake3:{}", digest.hex())
+}
+
+fn local_artifact_id_from_stack_digest(prefix: &str, material: &str) -> ArtifactId {
+    let digest = StackContentDigest::compute_str(material);
+    ArtifactId::new(format!("{prefix}:{}", digest.hex()))
+}
+
+// Display formatting only. Stack-owned digest computation must go through
+// `stack_ids::ContentDigest` and this string must not be used as identity.
+fn display_json_string(value: &serde_json::Value) -> String {
+    serde_json::to_string(value).unwrap_or_default()
 }
 
 #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, JsonSchema)]
@@ -645,7 +618,9 @@
             poison_id: generated_artifact_id("poison-receipt"),
             source_path: source_path.into(),
             line_number,
-            raw_digest: stable_json_digest(&serde_json::Value::String(raw_line.clone())),
+            raw_digest: non_authoritative_json_display_digest(&serde_json::Value::String(
+                raw_line.clone(),
+            )),
             raw_line,
             reason_codes: vec![reason.into()],
             recorded_at: Utc::now(),
@@ -1622,7 +1597,7 @@
     pub requested_path: String,
     pub resolved_path: String,
     pub bytes: u64,
-    pub content_digest: CanonicalDigestV1,
+    pub content_digest: DisplayDigestV1,
     pub allowed: bool,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub reason_codes: Vec<String>,
@@ -1645,7 +1620,7 @@
             requested_path: requested_path.into(),
             resolved_path: resolved_path.into(),
             bytes,
-            content_digest: CanonicalDigestV1::for_text(content),
+            content_digest: DisplayDigestV1::for_text(content),
             allowed: true,
             reason_codes: vec!["sandbox-read-allowed".into()],
             read_at: Utc::now(),
@@ -1666,7 +1641,7 @@
             requested_path: requested_path.into(),
             resolved_path: String::new(),
             bytes: 0,
-            content_digest: CanonicalDigestV1::for_text(""),
+            content_digest: DisplayDigestV1::for_text(""),
             allowed: false,
             reason_codes: vec![reason],
             read_at: Utc::now(),
@@ -1689,7 +1664,7 @@
     pub sandbox_root: String,
     pub requested_path: String,
     pub entries: Vec<RepoListEntryV1>,
-    pub listing_digest: CanonicalDigestV1,
+    pub listing_digest: DisplayDigestV1,
     pub allowed: bool,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub reason_codes: Vec<String>,
@@ -1710,7 +1685,7 @@
             sandbox_root: sandbox_root.into(),
             requested_path: requested_path.into(),
             entries,
-            listing_digest: CanonicalDigestV1::for_json_value(&listing),
+            listing_digest: DisplayDigestV1::for_json_value(&listing),
             allowed: true,
             reason_codes: vec!["sandbox-list-allowed".into()],
             listed_at: Utc::now(),
@@ -1729,7 +1704,7 @@
             sandbox_root: sandbox_root.into(),
             requested_path: requested_path.into(),
             entries: Vec::new(),
-            listing_digest: CanonicalDigestV1::for_json_value(&serde_json::json!([])),
+            listing_digest: DisplayDigestV1::for_json_value(&serde_json::json!([])),
             allowed: false,
             reason_codes: vec![reason.into()],
             listed_at: Utc::now(),
@@ -1744,7 +1719,7 @@
     pub summary: String,
     pub unified_diff: String,
     pub touched_paths: Vec<String>,
-    pub diff_digest: CanonicalDigestV1,
+    pub diff_digest: DisplayDigestV1,
     pub mutates_files: bool,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub reason_codes: Vec<String>,
@@ -1762,7 +1737,7 @@
             proposal_id: generated_artifact_id("patch-proposal"),
             kind: ArtifactKindV1::PatchProposal,
             summary: summary.into(),
-            diff_digest: CanonicalDigestV1::for_text(&unified_diff),
+            diff_digest: DisplayDigestV1::for_text(&unified_diff),
             unified_diff,
             touched_paths: sorted_unique_strings(touched_paths),
             mutates_files: false,
@@ -1782,9 +1757,9 @@
     pub permit_grant_id: Option<ArtifactId>,
     pub permit_use_receipt_id: Option<ArtifactId>,
     pub touched_paths: Vec<String>,
-    pub input_digest: CanonicalDigestV1,
-    pub before_digests: BTreeMap<String, CanonicalDigestV1>,
-    pub after_digests: BTreeMap<String, CanonicalDigestV1>,
+    pub input_digest: DisplayDigestV1,
+    pub before_digests: BTreeMap<String, DisplayDigestV1>,
+    pub after_digests: BTreeMap<String, DisplayDigestV1>,
     pub applied: bool,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub reason_codes: Vec<String>,
@@ -1796,8 +1771,8 @@
         sandbox_root: impl Into<String>,
         input: &serde_json::Value,
         touched_paths: Vec<String>,
-        before_digests: BTreeMap<String, CanonicalDigestV1>,
-        after_digests: BTreeMap<String, CanonicalDigestV1>,
+        before_digests: BTreeMap<String, DisplayDigestV1>,
+        after_digests: BTreeMap<String, DisplayDigestV1>,
         permit_grant_id: Option<ArtifactId>,
         permit_use_receipt_id: Option<ArtifactId>,
     ) -> Self {
@@ -1810,7 +1785,7 @@
             permit_grant_id,
             permit_use_receipt_id,
             touched_paths: sorted_unique_strings(touched_paths),
-            input_digest: CanonicalDigestV1::for_json_value(input),
+            input_digest: DisplayDigestV1::for_json_value(input),
             before_digests,
             after_digests,
             applied: true,
@@ -1833,7 +1808,7 @@
             permit_grant_id: None,
             permit_use_receipt_id: None,
             touched_paths: Vec::new(),
-            input_digest: CanonicalDigestV1::for_json_value(input),
+            input_digest: DisplayDigestV1::for_json_value(input),
             before_digests: BTreeMap::new(),
             after_digests: BTreeMap::new(),
             applied: false,
@@ -1854,8 +1829,8 @@
     pub permit_use_receipt_id: Option<ArtifactId>,
     pub allowed_by_policy: bool,
     pub exit_code: Option<i32>,
-    pub stdout_digest: CanonicalDigestV1,
-    pub stderr_digest: CanonicalDigestV1,
+    pub stdout_digest: DisplayDigestV1,
+    pub stderr_digest: DisplayDigestV1,
     pub stdout_bytes: usize,
     pub stderr_bytes: usize,
     pub timed_out: bool,
@@ -1887,8 +1862,8 @@
             permit_use_receipt_id,
             allowed_by_policy: true,
             exit_code,
-            stdout_digest: CanonicalDigestV1::for_text(stdout),
-            stderr_digest: CanonicalDigestV1::for_text(stderr),
+            stdout_digest: DisplayDigestV1::for_text(stdout),
+            stderr_digest: DisplayDigestV1::for_text(stderr),
             stdout_bytes: stdout.len(),
             stderr_bytes: stderr.len(),
             timed_out: false,
@@ -1919,8 +1894,8 @@
             permit_use_receipt_id: None,
             allowed_by_policy: false,
             exit_code: None,
-            stdout_digest: CanonicalDigestV1::for_text(""),
-            stderr_digest: CanonicalDigestV1::for_text(""),
+            stdout_digest: DisplayDigestV1::for_text(""),
+            stderr_digest: DisplayDigestV1::for_text(""),
             stdout_bytes: 0,
             stderr_bytes: 0,
             timed_out: false,
@@ -2110,15 +2085,15 @@
         wake_signal_id: Option<ArtifactId>,
     ) -> Self {
         let idempotency_key = idempotency_key.into();
-        let payload_digest = stable_json_digest(&payload);
+        let payload_digest = non_authoritative_json_display_digest(&payload);
         let identity_material =
             format!("{}|{}|{}", namespace_id.0, idempotency_key, payload_digest);
         let now = Utc::now();
         Self {
-            job_id: deterministic_artifact_id("job", &identity_material),
+            job_id: local_artifact_id_from_stack_digest("job", &identity_material),
             kind: ArtifactKindV1::Job,
             namespace_id,
-            attempt_family_id: deterministic_artifact_id(
+            attempt_family_id: local_artifact_id_from_stack_digest(
                 "attempt-family",
                 &format!("job-attempt-family|{identity_material}"),
             ),
@@ -2192,7 +2167,7 @@
             acquired_at.timestamp_nanos_opt().unwrap_or_default()
         );
         Self {
-            lease_id: deterministic_artifact_id("queue-lease", &material),
+            lease_id: local_artifact_id_from_stack_digest("queue-lease", &material),
             kind: ArtifactKindV1::QueueLease,
             namespace_id: job.namespace_id.clone(),
             job_id: job.job_id.clone(),
@@ -2237,13 +2212,16 @@
     ) -> Self {
         let schedule_id = schedule_id.into();
         let occurrence_key = occurrence_key.into();
-        let payload_digest = stable_json_digest(&payload);
+        let payload_digest = non_authoritative_json_display_digest(&payload);
         let idempotency_key = format!(
             "schedule:{}:{}:{}:{}",
             namespace_id.0, schedule_id, occurrence_key, payload_digest
         );
         Self {
-            occurrence_id: deterministic_artifact_id("schedule-occurrence", &idempotency_key),
+            occurrence_id: local_artifact_id_from_stack_digest(
+                "schedule-occurrence",
+                &idempotency_key,
+            ),
             kind: ArtifactKindV1::ScheduleOccurrence,
             namespace_id,
             schedule_id,
@@ -2290,13 +2268,13 @@
     ) -> Self {
         let source = source.into();
         let signal_key = signal_key.into();
-        let payload_digest = stable_json_digest(&payload);
+        let payload_digest = non_authoritative_json_display_digest(&payload);
         let idempotency_key = format!(
             "wake:{}:{}:{}:{}",
             namespace_id.0, source, signal_key, payload_digest
         );
         Self {
-            signal_id: deterministic_artifact_id("wake-signal", &idempotency_key),
+            signal_id: local_artifact_id_from_stack_digest("wake-signal", &idempotency_key),
             kind: ArtifactKindV1::WakeSignal,
             namespace_id,
             source,
@@ -2334,7 +2312,10 @@
         let daemon_owner = daemon_owner.into();
         let namespace_material = format!("{name}|{queue_root}|{daemon_owner}");
         Self {
-            namespace_id: deterministic_artifact_id("daemon-namespace", &namespace_material),
+            namespace_id: local_artifact_id_from_stack_digest(
+                "daemon-namespace",
+                &namespace_material,
+            ),
             kind: ArtifactKindV1::DaemonNamespace,
             name,
             queue_root,
@@ -2517,7 +2498,7 @@
             trust_root_id: generated_artifact_id("trust-root"),
             kind: ArtifactKindV1::TrustRoot,
             producer_id: producer_id.into(),
-            public_key_digest: stable_text_digest(public_key_material.as_ref()),
+            public_key_digest: non_authoritative_text_display_digest(public_key_material.as_ref()),
             status: TrustRootStatusV1::Active,
             federation_scope: federation_scope.into(),
             admission_policy_id: admission_policy_id.into(),
@@ -2567,7 +2548,7 @@
     pub kind: ArtifactKindV1,
     pub envelope_id: ArtifactId,
     pub subject_artifact_id: ArtifactId,
-    pub subject_digest: CanonicalDigestV1,
+    pub subject_digest: DisplayDigestV1,
     pub producer_id: String,
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub trust_root_id: Option<ArtifactId>,
@@ -2590,7 +2571,7 @@
     pub envelope_id: ArtifactId,
     pub admission_decision_id: ArtifactId,
     pub subject_artifact_id: ArtifactId,
-    pub subject_digest: CanonicalDigestV1,
+    pub subject_digest: DisplayDigestV1,
     pub advisory_only: bool,
     pub imported: bool,
     pub locally_promoted: bool,
@@ -2791,8 +2772,8 @@
     pub mechanism_program: Vec<String>,
     #[serde(default, skip_serializing_if = "BTreeMap::is_empty")]
     pub parameters: BTreeMap<String, serde_json::Value>,
-    pub structural_signature_digest: CanonicalDigestV1,
-    pub observational_signature_digest: CanonicalDigestV1,
+    pub structural_signature_digest: DisplayDigestV1,
+    pub observational_signature_digest: DisplayDigestV1,
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub simulator_contract_id: Option<ArtifactId>,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
@@ -2836,9 +2817,9 @@
             "mechanism_program": &mechanism_program,
         });
         Self {
-            mechanism_id: deterministic_artifact_id(
+            mechanism_id: local_artifact_id_from_stack_digest(
                 "mechanism-report",
-                &canonical_json_string(&structural_payload),
+                &display_json_string(&structural_payload),
             ),
             kind: ArtifactKindV1::MechanismReport,
             mechanism_kind,
@@ -2850,10 +2831,8 @@
             causal_claims,
             mechanism_program,
             parameters,
-            structural_signature_digest: CanonicalDigestV1::for_json_value(&structural_payload),
-            observational_signature_digest: CanonicalDigestV1::for_json_value(
-                &observational_payload,
-            ),
+            structural_signature_digest: DisplayDigestV1::for_json_value(&structural_payload),
+            observational_signature_digest: DisplayDigestV1::for_json_value(&observational_payload),
             simulator_contract_id: None,
             source_artifact_ids: Vec::new(),
             evidence_ids: Vec::new(),
@@ -2919,7 +2898,7 @@
     pub seed_policy: String,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub replay_command: Vec<String>,
-    pub simulator_digest: CanonicalDigestV1,
+    pub simulator_digest: DisplayDigestV1,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub reason_codes: Vec<String>,
     pub created_at: DateTime<Utc>,
@@ -2943,9 +2922,9 @@
             "replay_command": &replay_command,
         });
         Self {
-            contract_id: deterministic_artifact_id(
+            contract_id: local_artifact_id_from_stack_digest(
                 "simulator-contract",
-                &canonical_json_string(&digest_payload),
+                &display_json_string(&digest_payload),
             ),
             kind: ArtifactKindV1::SimulatorContract,
             name,
@@ -2955,7 +2934,7 @@
             deterministic: true,
             seed_policy: "fixed-seed-required-for-replay".into(),
             replay_command,
-            simulator_digest: CanonicalDigestV1::for_json_value(&digest_payload),
+            simulator_digest: DisplayDigestV1::for_json_value(&digest_payload),
             reason_codes: vec!["simulator-contract-defines-replay-boundary".into()],
             created_at: Utc::now(),
         }
@@ -2985,9 +2964,9 @@
     pub theory_id: Option<ArtifactId>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub simulator_contract_id: Option<ArtifactId>,
-    pub dataset_digest: CanonicalDigestV1,
-    pub parameter_digest: CanonicalDigestV1,
-    pub output_digest: CanonicalDigestV1,
+    pub dataset_digest: DisplayDigestV1,
+    pub parameter_digest: DisplayDigestV1,
+    pub output_digest: DisplayDigestV1,
     pub outcome: FitRunOutcomeV1,
     pub objective: String,
     #[serde(default, skip_serializing_if = "Option::is_none")]
@@ -3024,9 +3003,9 @@
             mechanism_id,
             theory_id: None,
             simulator_contract_id,
-            dataset_digest: CanonicalDigestV1::for_json_value(&dataset),
-            parameter_digest: CanonicalDigestV1::for_json_value(&parameters),
-            output_digest: CanonicalDigestV1::for_json_value(&output),
+            dataset_digest: DisplayDigestV1::for_json_value(&dataset),
+            parameter_digest: DisplayDigestV1::for_json_value(&parameters),
+            output_digest: DisplayDigestV1::for_json_value(&output),
             outcome: FitRunOutcomeV1::Succeeded,
             objective: objective.into(),
             score,
@@ -3592,7 +3571,7 @@
     pub source_claim_ids: Vec<ArtifactId>,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub source_evidence_ids: Vec<ArtifactId>,
-    pub digest: CanonicalDigestV1,
+    pub digest: DisplayDigestV1,
     pub rebuilt_from_authoritative_memory: bool,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub reason_codes: Vec<String>,
@@ -3616,7 +3595,7 @@
             source_episode_ids,
             source_claim_ids,
             source_evidence_ids,
-            digest: CanonicalDigestV1::for_json_value(&digest_payload),
+            digest: DisplayDigestV1::for_json_value(&digest_payload),
             rebuilt_from_authoritative_memory: true,
             reason_codes: vec!["projection-rebuild-from-authoritative-memory".into()],
             created_at: Utc::now(),
@@ -3851,7 +3830,7 @@
             step: step.into(),
             command: command.into(),
             passed: true,
-            output_digest: stable_text_digest(output),
+            output_digest: non_authoritative_text_display_digest(output),
             reason_codes: vec!["install-smoke-step-passed".into()],
         }
     }
@@ -3866,7 +3845,7 @@
             step: step.into(),
             command: command.into(),
             passed: false,
-            output_digest: stable_text_digest(output),
+            output_digest: non_authoritative_text_display_digest(output),
             reason_codes: vec![reason.into()],
         }
     }
@@ -4085,7 +4064,7 @@
             command: command.into(),
             status: GateCommandStatusV1::Failed,
             exit_code: Some(exit_code),
-            output_digest: Some(stable_text_digest(&output.into())),
+            output_digest: Some(non_authoritative_text_display_digest(&output.into())),
             evidence_ref: None,
             reason_codes: vec![reason.into()],
             completed_at: Utc::now(),
@@ -5168,8 +5147,8 @@
     pub region_id: ArtifactId,
     pub requested_node_ids: Vec<ArtifactId>,
     pub max_exact_nodes: u32,
-    pub approximate_digest: CanonicalDigestV1,
-    pub oracle_digest: CanonicalDigestV1,
+    pub approximate_digest: DisplayDigestV1,
+    pub oracle_digest: DisplayDigestV1,
     pub agreement: OracleAgreementV1,
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub disagreement_bound: Option<f64>,
@@ -5184,8 +5163,8 @@
         region_id: ArtifactId,
         mut requested_node_ids: Vec<ArtifactId>,
         max_exact_nodes: u32,
-        approximate_digest: CanonicalDigestV1,
-        oracle_digest: CanonicalDigestV1,
+        approximate_digest: DisplayDigestV1,
+        oracle_digest: DisplayDigestV1,
         disagreement_bound: Option<f64>,
     ) -> Self {
         requested_node_ids.sort();
@@ -5744,8 +5723,8 @@
     pub kind: ArtifactKindV1,
     pub plan_id: ArtifactId,
     pub invariant_budget_id: ArtifactId,
-    pub before_digest: CanonicalDigestV1,
-    pub after_digest: CanonicalDigestV1,
+    pub before_digest: DisplayDigestV1,
+    pub after_digest: DisplayDigestV1,
     pub checked_query_receipt_ids: Vec<ArtifactId>,
     pub preserved_invariants: Vec<PreservedInvariantV1>,
     pub as_of_queries_preserved: bool,
@@ -5762,8 +5741,8 @@
     pub fn new(
         plan: &SubtractionPlanV1,
         budget: &InvariantBudgetV1,
-        before_digest: CanonicalDigestV1,
-        after_digest: CanonicalDigestV1,
+        before_digest: DisplayDigestV1,
+        after_digest: DisplayDigestV1,
         checked_query_receipt_ids: Vec<ArtifactId>,
     ) -> Self {
         let digest_stable = before_digest == after_digest;
@@ -5821,8 +5800,8 @@
     pub invariant_budget_id: ArtifactId,
     pub history_report_id: ArtifactId,
     pub operator: SubtractionOperatorV1,
-    pub before_digest: CanonicalDigestV1,
-    pub after_digest: CanonicalDigestV1,
+    pub before_digest: DisplayDigestV1,
+    pub after_digest: DisplayDigestV1,
     pub compacted: bool,
     pub destructive_deletion: bool,
     pub receipt_compaction_approved: bool,
@@ -5997,50 +5976,53 @@
 }
 
 #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, JsonSchema)]
-pub struct CanonicalDigestV1 {
+pub struct DisplayDigestV1 {
     pub algorithm: String,
     pub canonicalization: String,
     pub digest: String,
+    #[serde(default = "default_true")]
+    pub non_authoritative: bool,
+    #[serde(default, skip_serializing_if = "Vec::is_empty")]
+    pub reason_codes: Vec<String>,
 }
 
-impl CanonicalDigestV1 {
+impl DisplayDigestV1 {
     pub fn for_json_value(value: &serde_json::Value) -> Self {
         Self {
-            algorithm: "sha256".into(),
-            canonicalization: "aidens-json-c14n-v1".into(),
-            digest: stable_json_digest(value),
+            algorithm: "blake3".into(),
+            canonicalization: "stack-ids-json-c14n-v1-display-only".into(),
+            digest: non_authoritative_json_display_digest(value),
+            non_authoritative: true,
+            reason_codes: vec!["display-only-not-artifact-identity".into()],
         }
     }
 
     pub fn for_text(text: &str) -> Self {
         Self {
-            algorithm: "sha256".into(),
-            canonicalization: "utf8-text-v1".into(),
-            digest: stable_text_digest(text),
+            algorithm: "blake3".into(),
+            canonicalization: "stack-ids-utf8-text-v1-display-only".into(),
+            digest: non_authoritative_text_display_digest(text),
+            non_authoritative: true,
+            reason_codes: vec!["display-only-not-artifact-identity".into()],
         }
     }
 
-    pub fn for_stack_content_digest(
+    pub fn from_stack_content_digest_for_display(
         digest: StackContentDigest,
         canonicalization: impl Into<String>,
     ) -> Self {
         Self {
             algorithm: "blake3".into(),
-            canonicalization: canonicalization.into(),
-            digest: digest.hex().to_string(),
+            canonicalization: format!("{}-display-only", canonicalization.into()),
+            digest: non_authoritative_display_digest_string(&digest),
+            non_authoritative: true,
+            reason_codes: vec!["display-only-not-artifact-identity".into()],
         }
     }
+}
 
-    pub fn as_stack_content_digest(&self) -> Result<StackContentDigest, String> {
-        if self.algorithm != "blake3" {
-            return Err(format!(
-                "canonical stack digest requires blake3, found {}",
-                self.algorithm
-            ));
-        }
-        StackContentDigest::from_hex(self.digest.trim_start_matches("blake3:"))
-            .map_err(|error| error.to_string())
-    }
+fn default_true() -> bool {
+    true
 }
 
 #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, JsonSchema)]
@@ -6082,9 +6064,9 @@
     pub before_raw_digest: Option<String>,
     pub after_raw_digest: Option<String>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
-    pub before_canonical_digest: Option<CanonicalDigestV1>,
+    pub before_display_digest: Option<DisplayDigestV1>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
-    pub after_canonical_digest: Option<CanonicalDigestV1>,
+    pub after_display_digest: Option<DisplayDigestV1>,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub treatment_critical_fields: Vec<String>,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
@@ -6107,8 +6089,8 @@
             degraded: false,
             before_raw_digest: None,
             after_raw_digest: None,
-            before_canonical_digest: None,
-            after_canonical_digest: None,
+            before_display_digest: None,
+            after_display_digest: None,
             treatment_critical_fields: Vec::new(),
             treatment_integrity_warnings: Vec::new(),
             hard_failed: false,
@@ -6152,8 +6134,8 @@
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub tool_id: Option<String>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
-    pub schema_digest: Option<CanonicalDigestV1>,
-    pub input_digest: CanonicalDigestV1,
+    pub schema_digest: Option<DisplayDigestV1>,
+    pub input_digest: DisplayDigestV1,
     pub valid: bool,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub errors: Vec<String>,
@@ -6175,8 +6157,8 @@
             run_id: None,
             attempt_id: None,
             tool_id: None,
-            schema_digest: schema.map(CanonicalDigestV1::for_json_value),
-            input_digest: CanonicalDigestV1::for_json_value(input),
+            schema_digest: schema.map(DisplayDigestV1::for_json_value),
+            input_digest: DisplayDigestV1::for_json_value(input),
             valid,
             errors,
             reason_codes: if valid {
@@ -6254,7 +6236,7 @@
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub value: Option<serde_json::Value>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
-    pub canonical_digest: Option<CanonicalDigestV1>,
+    pub display_digest: Option<DisplayDigestV1>,
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub duplicate_key_findings: Vec<DuplicateKeyFindingV1>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
@@ -6274,7 +6256,7 @@
             accepted: false,
             degraded: true,
             value: None,
-            canonical_digest: None,
+            display_digest: None,
             duplicate_key_findings: Vec::new(),
             schema_validation: None,
             repair_receipt: None,
@@ -6374,7 +6356,7 @@
         Self {
             manifest_id: ArtifactId("generated-schema-manifest:v1".into()),
             manifest_version: 1,
-            registry_digest: stable_json_digest(
+            registry_digest: non_authoritative_json_display_digest(
                 &serde_json::to_value(registry).unwrap_or(serde_json::Value::Null),
             ),
             schemas,
@@ -6405,7 +6387,7 @@
             version: self.registration.version,
             rust_type: self.registration.rust_type.clone(),
             schema_path: self.registration.schema_path.clone(),
-            schema_digest: stable_json_digest(&self.schema),
+            schema_digest: non_authoritative_json_display_digest(&self.schema),
         }
     }
 
@@ -6492,7 +6474,7 @@
         }
         Self {
             report_id: ArtifactId("schema-compatibility-report:v1".into()),
-            registry_digest: stable_json_digest(
+            registry_digest: non_authoritative_json_display_digest(
                 &serde_json::to_value(registry).unwrap_or(serde_json::Value::Null),
             ),
             compatible,
@@ -6721,8 +6703,8 @@
         let human_diff = format!(
             "{} mismatch at {path}: expected {}, actual {}",
             case.title,
-            canonical_json_string(&expected),
-            canonical_json_string(&actual)
+            display_json_string(&expected),
+            display_json_string(&actual)
         );
         Self {
             finding_id: generated_artifact_id("differential-conformance-finding"),
@@ -7099,12 +7081,12 @@
             "receipt-major-immutable"
         ),
         schema_document!(
-            "canonical-digest",
+            "display-digest",
             1,
-            CanonicalDigestV1,
-            "CanonicalDigestV1",
+            DisplayDigestV1,
+            "DisplayDigestV1",
             "P06",
-            Some("tests/fixtures/p06/canonical_digest_v1.json"),
+            Some("tests/fixtures/p06/display_digest_v1.json"),
             "algorithm-change-requires-new-version"
         ),
         schema_document!(
@@ -8467,13 +8449,16 @@
     }
 
     #[test]
-    fn canonical_stack_digest_is_exposed_without_sha_shadowing() {
+    fn stack_digest_is_exposed_as_non_authoritative_display_digest() {
         let digest = canonical_stack::digest_json(&serde_json::json!({"b": 2, "a": 1})).unwrap();
         assert_eq!(digest.hex().len(), 64);
-        let wrapped =
-            CanonicalDigestV1::for_stack_content_digest(digest.clone(), "stack-json-c14n-v1");
+        let wrapped = DisplayDigestV1::from_stack_content_digest_for_display(
+            digest.clone(),
+            "stack-json-c14n-v1",
+        );
         assert_eq!(wrapped.algorithm, "blake3");
-        assert_eq!(wrapped.as_stack_content_digest().unwrap(), digest);
+        assert!(wrapped.non_authoritative);
+        assert_eq!(wrapped.digest, format!("blake3:{}", digest.hex()));
     }
 
     #[test]
@@ -8671,7 +8656,7 @@
         assert_eq!(receipt.run_id, Some(context.run_id));
         assert_eq!(receipt.attempt_id, Some(context.attempt_id));
         assert_eq!(receipt.outcome, ToolInvocationOutcomeV1::Succeeded);
-        assert!(receipt.input_digest.starts_with("sha256:"));
+        assert!(receipt.input_digest.starts_with("blake3:"));
         assert!(receipt.output_digest.is_some());
     }
 
@@ -8822,7 +8807,7 @@
     }
 
     #[test]
-    fn p05_canonical_json_digest_is_stable_across_json_layout() {
+    fn p05_display_json_digest_is_stack_owned_and_stable_across_json_layout() {
         let pretty: serde_json::Value = serde_json::from_str(
             r#"{
               "z": ["b", "a"],
@@ -8833,14 +8818,18 @@
         let compact: serde_json::Value =
             serde_json::from_str(r#"{"a":{"ok":true,"n":1},"z":["b","a"]}"#).unwrap();
 
-        assert_eq!(stable_json_digest(&pretty), stable_json_digest(&compact));
+        assert_eq!(
+            non_authoritative_json_display_digest(&pretty),
+            non_authoritative_json_display_digest(&compact)
+        );
     }
 
     #[test]
     fn p05_remaining_artifact_constructors_produce_linkable_shapes() {
         let receipt_id = generated_artifact_id("receipt");
         let parent_receipt_id = generated_artifact_id("receipt");
-        let content_digest = stable_json_digest(&serde_json::json!({"tool":"read","ok":false}));
+        let content_digest =
+            non_authoritative_json_display_digest(&serde_json::json!({"tool":"read","ok":false}));
         let poison =
             PoisonReportEntryV1::new("receipt-transactions.ndjson", 7, "{bad}", "json-error");
         let graph = ExecutionLineageGraphV1::new(
@@ -8857,7 +8846,7 @@
             }],
         );
 
-        assert!(poison.raw_digest.starts_with("sha256:"));
+        assert!(poison.raw_digest.starts_with("blake3:"));
         assert_eq!(graph.nodes.len(), 1);
         assert_eq!(graph.edges.len(), 1);
     }
@@ -8866,7 +8855,7 @@
     fn p06_boundary_contracts_are_receipt_bearing_and_cryptographic() {
         let left = serde_json::json!({"b": 2, "a": {"n": 1}});
         let right = serde_json::json!({"a": {"n": 1}, "b": 2});
-        let digest = CanonicalDigestV1::for_json_value(&left);
+        let digest = DisplayDigestV1::for_json_value(&left);
         let duplicate = DuplicateKeyFindingV1::new("$", "a", Some(2), Some(9));
         let validation = SchemaValidationReportV1::new(
             Some(&serde_json::json!({"type":"object"})),
@@ -8879,10 +8868,10 @@
             changed: true,
             repair_kind: "json-substring-extracted".into(),
             degraded: true,
-            before_raw_digest: Some(stable_text_digest("prefix {\"a\":1}")),
-            after_raw_digest: Some(stable_text_digest("{\"a\":1}")),
-            before_canonical_digest: None,
-            after_canonical_digest: Some(CanonicalDigestV1::for_json_value(
+            before_raw_digest: Some(non_authoritative_text_display_digest("prefix {\"a\":1}")),
+            after_raw_digest: Some(non_authoritative_text_display_digest("{\"a\":1}")),
+            before_display_digest: None,
+            after_display_digest: Some(DisplayDigestV1::for_json_value(
                 &serde_json::json!({"a":1}),
             )),
             treatment_critical_fields: vec!["a".into()],
@@ -8897,7 +8886,7 @@
             accepted: false,
             degraded: true,
             value: Some(left.clone()),
-            canonical_digest: Some(digest.clone()),
+            display_digest: Some(digest.clone()),
             duplicate_key_findings: vec![duplicate],
             schema_validation: Some(validation),
             repair_receipt: Some(repair),
@@ -8905,9 +8894,10 @@
             compiled_at: Utc::now(),
         };
 
-        assert_eq!(digest.algorithm, "sha256");
-        assert_eq!(digest, CanonicalDigestV1::for_json_value(&right));
-        assert!(digest.digest.starts_with("sha256:"));
+        assert_eq!(digest.algorithm, "blake3");
+        assert!(digest.non_authoritative);
+        assert_eq!(digest, DisplayDigestV1::for_json_value(&right));
+        assert!(digest.digest.starts_with("blake3:"));
         assert_eq!(
             outcome.schema_validation.unwrap().kind,
             ArtifactKindV1::SchemaValidation
@@ -9102,7 +9092,7 @@
         let poison = include_str!("../../../tests/fixtures/p05/poison_receipt_record_v1.json");
         let poison: PoisonReportEntryV1 = serde_json::from_str(poison).unwrap();
         assert_eq!(poison.line_number, 1);
-        assert!(poison.raw_digest.starts_with("sha256:"));
+        assert!(poison.raw_digest.starts_with("blake3:"));
 
         let graph = include_str!("../../../tests/fixtures/p05/execution_lineage_graph_v1.json");
         let graph: ExecutionLineageGraphV1 = serde_json::from_str(graph).unwrap();
@@ -9138,9 +9128,10 @@
         let duplicate: DuplicateKeyFindingV1 = serde_json::from_str(duplicate).unwrap();
         assert_eq!(duplicate.key, "path");
 
-        let digest = include_str!("../../../tests/fixtures/p06/canonical_digest_v1.json");
-        let digest: CanonicalDigestV1 = serde_json::from_str(digest).unwrap();
-        assert_eq!(digest.algorithm, "sha256");
+        let digest = include_str!("../../../tests/fixtures/p06/display_digest_v1.json");
+        let digest: DisplayDigestV1 = serde_json::from_str(digest).unwrap();
+        assert_eq!(digest.algorithm, "blake3");
+        assert!(digest.non_authoritative);
     }
 
     #[test]
@@ -9163,7 +9154,7 @@
         assert!(registry.contains_family_version("schema-compatibility-report", 1));
         assert_eq!(registry.families.len(), documents.len());
         assert_eq!(manifest.schemas.len(), documents.len());
-        assert!(manifest.registry_digest.starts_with("sha256:"));
+        assert!(manifest.registry_digest.starts_with("blake3:"));
         assert_eq!(
             plan.phases,
             vec![
@@ -9239,7 +9230,7 @@
             "tests/fixtures/p06/schema_validation_receipt_v1.json",
             "tests/fixtures/p06/json_repair_receipt_v2.json",
             "tests/fixtures/p06/duplicate_key_finding_v1.json",
-            "tests/fixtures/p06/canonical_digest_v1.json",
+            "tests/fixtures/p06/display_digest_v1.json",
         ];
 
         let _: SourceBasisLockV1 = serde_json::from_str(include_str!(
@@ -9374,8 +9365,8 @@
             "../../../tests/fixtures/p06/duplicate_key_finding_v1.json"
         ))
         .unwrap();
-        let _: CanonicalDigestV1 = serde_json::from_str(include_str!(
-            "../../../tests/fixtures/p06/canonical_digest_v1.json"
+        let _: DisplayDigestV1 = serde_json::from_str(include_str!(
+            "../../../tests/fixtures/p06/display_digest_v1.json"
         ))
         .unwrap();
 
@@ -9938,7 +9929,7 @@
             ArtifactId("contradiction-witness:test".into()),
             vec![claim_a, claim_b],
         );
-        let digest = CanonicalDigestV1::for_json_value(&serde_json::json!({
+        let digest = DisplayDigestV1::for_json_value(&serde_json::json!({
             "region": "test",
             "claims": 2
         }));
@@ -10061,7 +10052,7 @@
             &lawful_frontier,
             &budget,
         );
-        let digest = CanonicalDigestV1::for_json_value(&serde_json::json!({
+        let digest = DisplayDigestV1::for_json_value(&serde_json::json!({
             "claims": [accepted_claim]
         }));
         let report = HistoryPreservationReportV1::new(
@@ -10288,7 +10279,7 @@
                 "schemas/generated_schema_manifest_v1.json",
                 ReleaseArtifactKindV1::Manifest,
                 true,
-                "sha256:fixture",
+                "blake3:fixture",
                 12,
             )],
         );
--- .codex_evidence/contract_ownership/04/pre_edit_files/aidens_contracts_Cargo.toml.before	2026-04-29 04:04:00.951611900 -0500
+++ crates/aidens-contracts/Cargo.toml	2026-04-29 04:04:49.655364838 -0500
@@ -22,7 +22,6 @@
 semantic-memory-forge.workspace = true
 serde.workspace = true
 serde_json.workspace = true
-sha2.workspace = true
 stack-ids.workspace = true
 uuid.workspace = true
 verification-adjudication.workspace = true
--- .codex_evidence/contract_ownership/04/pre_edit_files/aidens_boundary_kit_lib.rs.before	2026-04-29 04:04:00.952611895 -0500
+++ crates/aidens-boundary-kit/src/lib.rs	2026-04-29 04:05:26.655168232 -0500
@@ -4,9 +4,9 @@
 //! JSON should pass through this crate before reaching tools or memory.
 
 use aidens_contracts::{
-    stable_json_digest, stable_text_digest, ArtifactId, BoundaryCompileOutcomeV1,
-    BoundaryCompileRequestV1, BoundaryRepairReportV1, CanonicalDigestV1, DuplicateKeyFindingV1,
-    JsonRepairReportV2, SchemaValidationReportV1,
+    non_authoritative_json_display_digest, non_authoritative_text_display_digest, ArtifactId,
+    BoundaryCompileOutcomeV1, BoundaryCompileRequestV1, BoundaryRepairReportV1, DisplayDigestV1,
+    DuplicateKeyFindingV1, JsonRepairReportV2, SchemaValidationReportV1,
 };
 use serde_json::Value;
 use std::collections::{BTreeMap, BTreeSet};
@@ -196,7 +196,7 @@
                     vec!["treatment-integrity-hard-fail".into()],
                 );
                 outcome.value = Some(value.clone());
-                outcome.canonical_digest = Some(CanonicalDigestV1::for_json_value(&value));
+                outcome.display_digest = Some(DisplayDigestV1::for_json_value(&value));
                 outcome.repair_receipt = Some(repair);
                 return outcome;
             }
@@ -272,7 +272,7 @@
                 .map(|receipt| receipt.degraded)
                 .unwrap_or(false),
         value: Some(value.clone()),
-        canonical_digest: Some(CanonicalDigestV1::for_json_value(&value)),
+        display_digest: Some(DisplayDigestV1::for_json_value(&value)),
         duplicate_key_findings: Vec::new(),
         schema_validation,
         repair_receipt,
@@ -292,7 +292,7 @@
         accepted: false,
         degraded: true,
         value: None,
-        canonical_digest: None,
+        display_digest: None,
         duplicate_key_findings: findings,
         schema_validation: None,
         repair_receipt,
@@ -322,10 +322,10 @@
         changed: before != after,
         repair_kind: repair_kind.into(),
         degraded: before != after,
-        before_raw_digest: Some(stable_text_digest(before)),
-        after_raw_digest: Some(stable_text_digest(after)),
-        before_canonical_digest: before_value.map(CanonicalDigestV1::for_json_value),
-        after_canonical_digest: after_value.map(CanonicalDigestV1::for_json_value),
+        before_raw_digest: Some(non_authoritative_text_display_digest(before)),
+        after_raw_digest: Some(non_authoritative_text_display_digest(after)),
+        before_display_digest: before_value.map(DisplayDigestV1::for_json_value),
+        after_display_digest: after_value.map(DisplayDigestV1::for_json_value),
         treatment_critical_fields: treatment_critical_fields.to_vec(),
         treatment_integrity_warnings,
         hard_failed: false,
@@ -546,12 +546,12 @@
     None
 }
 
-pub fn canonical_json_digest(value: &Value) -> String {
-    stable_json_digest(value)
+pub fn non_authoritative_boundary_json_display_digest(value: &Value) -> String {
+    non_authoritative_json_display_digest(value)
 }
 
 fn text_digest(text: &str) -> String {
-    stable_text_digest(text)
+    non_authoritative_text_display_digest(text)
 }
 
 pub fn no_repair_receipt() -> BoundaryRepairReportV1 {
@@ -940,7 +940,7 @@
     }
 
     #[test]
-    fn canonical_digest_is_stable_across_key_order_and_whitespace() {
+    fn display_digest_is_stable_across_key_order_and_whitespace() {
         let left: Value = serde_json::from_str(r#"{"b":2,"a":{"z":false,"n":1}}"#).unwrap();
         let right: Value = serde_json::from_str(
             r#"{
@@ -950,11 +950,14 @@
         )
         .unwrap();
 
-        assert_eq!(canonical_json_digest(&left), canonical_json_digest(&right));
-        assert!(canonical_json_digest(&left).starts_with("sha256:"));
         assert_eq!(
-            CanonicalDigestV1::for_json_value(&left),
-            CanonicalDigestV1::for_json_value(&right)
+            non_authoritative_boundary_json_display_digest(&left),
+            non_authoritative_boundary_json_display_digest(&right)
+        );
+        assert!(non_authoritative_boundary_json_display_digest(&left).starts_with("blake3:"));
+        assert_eq!(
+            DisplayDigestV1::for_json_value(&left),
+            DisplayDigestV1::for_json_value(&right)
         );
     }
 
--- .codex_evidence/contract_ownership/04/pre_edit_files/aidens_tool_kit_lib.rs.before	2026-04-29 04:04:00.953611889 -0500
+++ crates/aidens-tool-kit/src/lib.rs	2026-04-29 04:07:21.895547703 -0500
@@ -2,12 +2,12 @@
 
 use aidens_boundary_kit::validate_tool_input_schema;
 use aidens_contracts::{
-    ApprovalRequestV1, ArtifactId, CanonicalDigestV1, CanonicalToolSideEffectClass,
-    CapabilityGateDecisionDraftV1, CapabilityGateDecisionV1, CapabilityGateOutcomeV1,
-    CommandRunReportV1, PatchApplyReportV1, PatchProposalV1, PermitUseReportV1,
-    ProviderRouteReportV1, RepoListEntryV1, RepoListReportV1, RepoReadReportV1,
-    SchemaValidationReportV1, ToolDescriptorV1, ToolExposureSetV1, ToolInvocationReportV1,
-    ToolLifecycleStateV1, ToolProviderSchemaV1, ToolSchemaV1,
+    ApprovalRequestV1, ArtifactId, CanonicalToolSideEffectClass, CapabilityGateDecisionDraftV1,
+    CapabilityGateDecisionV1, CapabilityGateOutcomeV1, CommandRunReportV1, DisplayDigestV1,
+    PatchApplyReportV1, PatchProposalV1, PermitUseReportV1, ProviderRouteReportV1, RepoListEntryV1,
+    RepoListReportV1, RepoReadReportV1, SchemaValidationReportV1, ToolDescriptorV1,
+    ToolExposureSetV1, ToolInvocationReportV1, ToolLifecycleStateV1, ToolProviderSchemaV1,
+    ToolSchemaV1,
 };
 use aidens_permit_kit::{requires_permit, PermitCheckContextV1, PermitDecisionV1, PermitPolicyV1};
 use aidens_security_kit::{validate_sandbox_path, PathSafetyError};
@@ -1029,7 +1029,7 @@
         .with_context(|| format!("file-stat cannot stat {}", resolved.display()))?;
     let content_digest = if metadata.is_file() && metadata.len() <= 1_048_576 {
         let content = std::fs::read_to_string(&resolved).unwrap_or_default();
-        Some(CanonicalDigestV1::for_text(&content))
+        Some(DisplayDigestV1::for_text(&content))
     } else {
         None
     };
@@ -1115,8 +1115,8 @@
         std::fs::write(&path, &after)
             .with_context(|| format!("failed to write patched file {}", path.display()))?;
         let display_path = display_sandbox_path(sandbox_root, &path);
-        before_digests.insert(display_path.clone(), CanonicalDigestV1::for_text(&before));
-        after_digests.insert(display_path.clone(), CanonicalDigestV1::for_text(&after));
+        before_digests.insert(display_path.clone(), DisplayDigestV1::for_text(&before));
+        after_digests.insert(display_path.clone(), DisplayDigestV1::for_text(&after));
         touched_paths.push(display_path);
     }
 
--- .codex_evidence/contract_ownership/04/pre_edit_files/aidens_cli_lib.rs.before	2026-04-29 04:04:00.954611885 -0500
+++ crates/aidens-cli/src/lib.rs	2026-04-29 04:07:21.768548387 -0500
@@ -4,7 +4,7 @@
 use aidens_config::{load_config_file, AiDENsConfigV1, ProviderConfigV1};
 use aidens_contracts::{
     current_artifact_family_registry, generated_schema_documents, generated_schema_manifest,
-    generated_schema_manifest_pretty_json, stable_text_digest, AiDENsAppPlanV1,
+    generated_schema_manifest_pretty_json, non_authoritative_text_display_digest, AiDENsAppPlanV1,
     AiDENsCompiledPlanV1, AiDENsDoctorReportV1, ApprovalDecisionV1, ApprovalRequestV1, ArtifactId,
     BoundaryCompileRequestV1, CanonicalToolSideEffectClass, CapabilityStateV1, CodexPacketInputV1,
     CodexPacketV1, CommandRunReportV1, CompletionAuditReportV1, ConfigApplyReportDraftV1,
@@ -1324,7 +1324,7 @@
         let bytes =
             std::fs::read(&path).with_context(|| format!("failed to read {}", path.display()))?;
         (
-            stable_text_digest(&String::from_utf8_lossy(&bytes)),
+            non_authoritative_text_display_digest(&String::from_utf8_lossy(&bytes)),
             bytes.len() as u64,
         )
     };
@@ -1346,10 +1346,15 @@
         let relative = relative_path(root, &path)?;
         material.push_str(&relative);
         material.push('\0');
-        material.push_str(&stable_text_digest(&String::from_utf8_lossy(&bytes)));
+        material.push_str(&non_authoritative_text_display_digest(
+            &String::from_utf8_lossy(&bytes),
+        ));
         material.push('\n');
     }
-    Ok((stable_text_digest(&material), bytes_total))
+    Ok((
+        non_authoritative_text_display_digest(&material),
+        bytes_total,
+    ))
 }
 
 fn collect_files_recursive(root: &Path, files: &mut Vec<PathBuf>) -> Result<()> {
--- .codex_evidence/contract_ownership/04/pre_edit_files/aidens_runner_lib.rs.before	2026-04-29 04:04:00.955611879 -0500
+++ crates/aidens-runner/src/lib.rs	2026-04-29 04:05:42.823081178 -0500
@@ -1384,7 +1384,7 @@
         assert!(invocation.succeeded);
         assert!(invocation.run_id.is_some());
         assert!(invocation.attempt_id.is_some());
-        assert!(invocation.input_digest.starts_with("sha256:"));
+        assert!(invocation.input_digest.starts_with("blake3:"));
         assert!(invocation.output_digest.is_some());
         assert_eq!(output.receipt.tool_call_requests.len(), 1);
         assert_eq!(output.receipt.tool_call_results.len(), 1);
--- .codex_evidence/contract_ownership/04/pre_edit_files/boundary_compile_outcome_v1.json.before	2026-04-29 04:04:00.956611874 -0500
+++ tests/fixtures/p06/boundary_compile_outcome_v1.json	2026-04-29 04:06:20.662707284 -0500
@@ -6,24 +6,24 @@
   "value": {
     "path": 7
   },
-  "canonical_digest": {
-    "algorithm": "sha256",
-    "canonicalization": "aidens-json-c14n-v1",
-    "digest": "sha256:d826062fb4772d1122c14809cbffb52c7cca418c310f91e63e1dad553ae92e24"
+  "display_digest": {
+    "algorithm": "blake3",
+    "canonicalization": "stack-ids-json-c14n-v1-display-only",
+    "digest": "blake3:d826062fb4772d1122c14809cbffb52c7cca418c310f91e63e1dad553ae92e24"
   },
   "schema_validation": {
     "receipt_id": "schema-validation:fixture",
     "kind": "schema-validation",
     "tool_id": "aidens:repo-read:1",
     "schema_digest": {
-      "algorithm": "sha256",
-      "canonicalization": "aidens-json-c14n-v1",
-      "digest": "sha256:39b714704935190561ed407980480b9a4a0b346b97346e0bff71fb9ace820194"
+      "algorithm": "blake3",
+      "canonicalization": "stack-ids-json-c14n-v1-display-only",
+      "digest": "blake3:39b714704935190561ed407980480b9a4a0b346b97346e0bff71fb9ace820194"
     },
     "input_digest": {
-      "algorithm": "sha256",
-      "canonicalization": "aidens-json-c14n-v1",
-      "digest": "sha256:d826062fb4772d1122c14809cbffb52c7cca418c310f91e63e1dad553ae92e24"
+      "algorithm": "blake3",
+      "canonicalization": "stack-ids-json-c14n-v1-display-only",
+      "digest": "blake3:d826062fb4772d1122c14809cbffb52c7cca418c310f91e63e1dad553ae92e24"
     },
     "valid": false,
     "errors": [
--- .codex_evidence/contract_ownership/04/pre_edit_files/json_repair_receipt_v2.json.before	2026-04-29 04:04:00.957611869 -0500
+++ tests/fixtures/p06/json_repair_receipt_v2.json	2026-04-29 04:06:20.662816308 -0500
@@ -4,12 +4,12 @@
   "changed": true,
   "repair_kind": "json-substring-extracted",
   "degraded": true,
-  "before_raw_digest": "sha256:4171ebd6c211b30504b78c9057f743436a37dac97b5aac1c7c59af0e62602012",
-  "after_raw_digest": "sha256:e74601c36489aa2eb1837c89afe84dd892caaed36b33a9dfd63ec8445d4b9713",
-  "after_canonical_digest": {
-    "algorithm": "sha256",
-    "canonicalization": "aidens-json-c14n-v1",
-    "digest": "sha256:e74601c36489aa2eb1837c89afe84dd892caaed36b33a9dfd63ec8445d4b9713"
+  "before_raw_digest": "blake3:4171ebd6c211b30504b78c9057f743436a37dac97b5aac1c7c59af0e62602012",
+  "after_raw_digest": "blake3:e74601c36489aa2eb1837c89afe84dd892caaed36b33a9dfd63ec8445d4b9713",
+  "after_display_digest": {
+    "algorithm": "blake3",
+    "canonicalization": "stack-ids-json-c14n-v1-display-only",
+    "digest": "blake3:e74601c36489aa2eb1837c89afe84dd892caaed36b33a9dfd63ec8445d4b9713"
   },
   "treatment_critical_fields": [
     "treatment"
--- .codex_evidence/contract_ownership/04/pre_edit_files/canonical_digest_v1.json.before	2026-04-29 04:04:00.958611864 -0500
+++ tests/fixtures/p06/display_digest_v1.json	2026-04-29 04:06:20.662916165 -0500
@@ -1,5 +1,9 @@
 {
-  "algorithm": "sha256",
-  "canonicalization": "aidens-json-c14n-v1",
-  "digest": "sha256:7d6441497d2a000b8143602a7817c90abe7db88e139f89c062a1c36cfe0ad9d6"
+  "algorithm": "blake3",
+  "canonicalization": "stack-ids-json-c14n-v1-display-only",
+  "digest": "blake3:7d6441497d2a000b8143602a7817c90abe7db88e139f89c062a1c36cfe0ad9d6",
+  "non_authoritative": true,
+  "reason_codes": [
+    "display-only-not-artifact-identity"
+  ]
 }
--- .codex_evidence/contract_ownership/04/pre_edit_files/projection_digest_v1.json.before	2026-04-29 04:04:00.959611859 -0500
+++ tests/fixtures/p13/projection_digest_v1.json	2026-04-29 04:06:20.666309574 -0500
@@ -13,9 +13,9 @@
     "evidence:p13"
   ],
   "digest": {
-    "algorithm": "sha256",
-    "canonicalization": "aidens-json-c14n-v1",
-    "digest": "sha256:2222222222222222222222222222222222222222222222222222222222222222"
+    "algorithm": "blake3",
+    "canonicalization": "stack-ids-json-c14n-v1-display-only",
+    "digest": "blake3:2222222222222222222222222222222222222222222222222222222222222222"
   },
   "rebuilt_from_authoritative_memory": true,
   "reason_codes": [
--- /dev/null	2026-04-22 13:11:05.642527016 -0500
+++ docs/contract-ownership/DIGEST_IDENTITY_SOURCE_OF_TRUTH.md	2026-04-29 04:08:28.382189675 -0500
@@ -0,0 +1,28 @@
+# Digest Identity Source Of Truth
+
+SOURCE BASIS: 2026-04-28
+
+Canonical digest and content-addressing law is owned by `~/Coding/Libraries/stack-ids`.
+
+AiDENs may use:
+
+- `stack_ids::ContentDigest` and `stack_ids::DigestBuilder` for stack-owned digest computation;
+- `stack_ids::ArtifactId` and sibling opaque ID types for typed identifiers;
+- `DisplayDigestV1` only as a non-authoritative report/display wrapper.
+
+AiDENs must not export local canonical digest or ID law. The removed local law names are:
+
+- `stable_json_digest`
+- `stable_text_digest`
+- `deterministic_artifact_id`
+- `canonical_json_string`
+- `CanonicalDigestV1`
+
+Current AiDENs display digest rule:
+
+- digest bytes are computed by `stack_ids::ContentDigest`;
+- serialized display strings use a `blake3:` prefix for operator readability;
+- `DisplayDigestV1.non_authoritative` is always true for constructors;
+- `DisplayDigestV1` is not an artifact identity type and must not be used as a canonical stack content address.
+
+AiDENs-local idempotency IDs for queue, schedule, wake, and report wrappers may derive local `ArtifactId` strings from `stack_ids::ContentDigest`, but they do not define stack-wide artifact identity law.
