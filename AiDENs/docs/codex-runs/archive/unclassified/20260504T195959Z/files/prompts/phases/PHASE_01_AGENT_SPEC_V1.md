# P26 Phase 01 — AgentSpecV1 Contract

Implement `AgentSpecV1` with schema, fixtures, validation, docs, and support policy checks.

Must reject unsupported provider/cloud/autonomy paths and unbounded budgets.

Emit phase report and STOP for gate after this phase.
