# Phase 01 injection — restore missing package files

Focus only on package/file integrity.

Required:

- restore `evals/p20_agency_eval_cases.jsonl`;
- restore or remove missing manifest paths;
- validate agency cases;
- rerun include/manifest checks.

Forbidden:

- deleting tests to hide missing fixtures;
- weakening agency eval requirements;
- regenerating manifest without explaining removed entries.
