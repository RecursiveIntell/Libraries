# P25 Flagship Fixture Repo

This fixture is fixture-backed evidence for the P25 flagship local coding-agent demo.

It intentionally remains tiny and deterministic:

- `add_one` helper in `src/lib.rs`.
- No network calls.
- No cloud execution.
