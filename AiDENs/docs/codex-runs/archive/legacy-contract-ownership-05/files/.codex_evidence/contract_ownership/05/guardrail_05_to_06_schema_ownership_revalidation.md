# Guardrail 05 To 06 - Schema Ownership Revalidation

Date: 2026-04-29
Working directory: `/home/sikmindz/Coding/Libraries/AiDENs`

## 1. Canonical Schema Owner

Canonical artifact family schemas are generated by owner crates through `~/Coding/Libraries/contract-schema-gen`.

Evidence saved in `guardrail_contract_schema_gen_owner_scan.txt` shows `contract-schema-gen` registrations for:

- `attestation_exchange::AttestationEnvelopeV1`
- `federated_settlement::SharedDispositionV1`
- `federated_settlement::SettlementCaseV1`
- `mechanism_runtime::TheoryVersionV1`
- `mechanism_runtime::HypothesisLibraryV1`
- `mechanism_runtime::TheoryRefuterSuiteV1`
- memory/evidence families including `episode-bundle-v1`, `evidence-bundle-v1`, and `execution-context-v1`
- verification/repair families including `verification-plan-artifact-v1`, `repair-record-v1`, `boundary-repair-record-v1`, and `verification-disposition-v1`

## 2. AiDENs Schema Output Scope

AiDENs generated schema manifest evidence is saved in `guardrail_aidens_manifest_local_schema_list.txt`.

Current generated AiDENs schema count:

```text
schema_count=58
manifest_reason_codes=aidens-local-display-report-schemas-only
```

The manifest contains AiDENs-local product, display, report, CLI, operator, queue, schedule, wake, permit, provider, release, and schema-governance DTOs only.

## 3. No Canonical Family Emission

Forbidden generated schema path/content scans are saved in `guardrail_forbidden_schema_scan.txt`.

Results:

```text
path_scan_exit_status=1
content_scan_exit_status=1
forbidden_manifest_family_hits=0
```

Exit status `1` from `rg` means no matching forbidden generated schema path/content was found. AiDENs does not emit generated `*.schema.json` files for federation, attestation, mechanism, memory, kernel, verification, canonical repair, or canonical digest law.

`display-digest/v1.schema.json` remains only as the non-authoritative `DisplayDigestV1` display/report DTO and is not canonical digest law.

## 4. Schema Scope Gate

Saved in `guardrail_schema_scope_gate.txt`:

```text
PASS: schema generation scope appears AiDENs-local/non-authoritative (registered_families=58, checked_schema_files=58).
EXIT_STATUS=0
```

## 5. Quarantine

Schema ambiguity is quarantined in `docs/contract-ownership/quarantine/phase05-schema-sketches.md` and listed in `docs/contract-ownership/FINAL_QUARANTINE_LEDGER.md`.

The quarantined files are historical `*.sketch.json` files, not generated `*.schema.json` files:

- `schemas/core_artifact_header_v1.sketch.json`
- `schemas/execution_lineage_graph_v1.sketch.json`
- `schemas/poison_receipt_record_v1.sketch.json`

## Halt Decision

AiDENs is not currently a schema authority for canonical families. Phase 06 may start only after this guardrail is accepted.
