# Phase 0 Prompt — Inventory and Stop-Rule Check

Inspect the repository without modifying code except for `handoffs/P31A_PHASE_00_INVENTORY.md`.

Report:

- all protected docs and their run/status/support/package/build claims;
- existing current-run ledger files;
- stale defaults in verifier/package scripts;
- root Markdown ambiguity count;
- Codex artifact classification gaps;
- current CI workflow;
- exact packaging script and supported CLI options;
- cargo/rust availability;
- any blocker that prevents P31A from proceeding.

Do not normalize docs yet. Do not implement runtime features. Stop after writing the inventory.
