app_id = "aidens-next-mock"
profile_id = "coding-agent"
memory_mode = "disabled"
receipt_level = "full"

[provider]
kind = "mock"
model = "mock-model"
mock_response = "mock provider response from AiDENs"

[tools]
sandbox_root = "."
enabled_bundles = ["safe-coding"]

[security]
approval_mode = "require-side-effects"
network_policy = "disabled"
write_policy = "approval-required"
