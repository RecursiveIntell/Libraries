[package]
name = "aidens-wake-kit"
version = "0.1.0"
edition = "2021"
rust-version = "1.76"
license = "MIT OR Apache-2.0"
description = "Wake-signal construction for daemon queues"


[dependencies]
aidens-contracts = { path = "../aidens-contracts" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
