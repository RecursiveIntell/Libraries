[package]
name = "aidens-permit-kit"
version = "0.1.0"
edition = "2021"
rust-version = "1.76"
license = "MIT OR Apache-2.0"
description = "Approval and permit model for tool dispatch gating"


[dependencies]
aidens-contracts = { path = "../aidens-contracts" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
aidens-testkit = { path = "../aidens-testkit" }
serde_json = "1"
