[package]
name = "aidens-profile-memory"
version = "0.1.0"
edition = "2021"
rust-version = "1.76"
license = "MIT OR Apache-2.0"
description = "Memory-agent profile status (partial/proof-only surface)"


[dependencies]
aidens-memory-kit = { version = "0.1.0", path = "../aidens-memory-kit" }
