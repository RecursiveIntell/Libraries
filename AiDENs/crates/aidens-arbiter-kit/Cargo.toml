[package]
name = "aidens-arbiter-kit"
version = "0.1.0"
edition = "2021"
rust-version = "1.76"
license = "MIT OR Apache-2.0"
description = "Thin arbiter facade over canonical contradiction artifacts"


[dependencies]
semantic-memory-forge = { version = "0.1.0", path = "../../../semantic-memory-forge" }
stack-ids = { version = "0.1.0", path = "../../../stack-ids" }

[dev-dependencies]
