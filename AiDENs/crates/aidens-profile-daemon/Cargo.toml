[package]
name = "aidens-profile-daemon"
version = "0.1.0"
edition = "2021"
rust-version = "1.76"
license = "MIT OR Apache-2.0"
description = "Daemon profile status — safe-mode surface for autonomous operation"


[dependencies]
