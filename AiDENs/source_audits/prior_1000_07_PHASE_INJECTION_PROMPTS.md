# 07 — Manual Phase-Injection Prompts

## Injection after phase 1: Receipt/log durability and no done without receipts

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E01, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 2: Transactional patch and command execution hardening

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E02, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 3: Sandbox and security hostile corpus

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E03, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 4: Provider honesty and route discipline

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E04, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 5: Boundary compiler and schema governance

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E05, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 6: Queue/daemon concurrency

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E06, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 7: Bitemporal/proof/view reference corpus

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E07, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 8: Minimal v11B regional runtime slice

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E08, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 9: Modularity/source-of-truth cleanup

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E09, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

## Injection after phase 10: Docs/matrix/support evidence closure

Paste this before Codex proceeds:

```text
Stop and revalidate invariants before proceeding. For the work just completed under E10, prove with file paths and command outputs that:
1. no shadow truth/store/adapter semantics were introduced;
2. every material operation changed or added is receipt-bearing;
3. no proof waiver is treated as proof;
4. no degraded path reports as exact;
5. no user-visible done state is emitted before required receipts;
6. all new tests are semantic/negative where possible, not marker-only;
7. unresolved items are marked open/quarantined/deferred with issue IDs.
If any item fails, halt and repair/quarantine before moving on.
```

