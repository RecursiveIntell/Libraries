# P30 Operator Paste-First Prompt — v11B-centered correction

You are executing AiDENs P30. Treat this as a **v11B-centered hostile audit absorption super-pass**.

The previous P30 framing treated v11B as a late seed. That is now corrected:

- v11A is the prerequisite constitutional microkernel law.
- v11B is the active organizing runtime target.
- v11C remains reserved unless a surface is explicitly activated and gated.

Do not proceed as if the goal is merely to add receipts and schemas. The pass must move AiDENs toward the v11B runtime geometry:

1. declared graph surfaces;
2. right-graph conformance;
3. region contracts;
4. typed region boundary messages and receipts;
5. replay slices and region state snapshots;
6. residual/syndrome envelopes;
7. convergence reports and stop-law enforcement;
8. local repair candidates and repair execution receipts;
9. support cores, removal frontiers, invariant-preservation receipts, and historical-loss budgets;
10. causal/interventional bundles where causal or attribution claims exist.

Hard boundary: do not fake v11B by adding inert structs. A v11B artifact without a production path, fixture, or explicit release debt is decorative. Decorative compliance is a violation.

Before any code change:

- read `P30_CODEX_SUPER_PASS_PROMPT.md`;
- read `P30_V11B_EXECUTION_SPINE.md`;
- read `P30_PHASE_PLAN.md`;
- run `scripts/p30_guard.py --repo .` if possible;
- create `handoffs/p30/P30-00_REPORT.md` with source-basis lock, current failures, and a go/no-go statement.

At every phase boundary, wait for the manual injection. If the injection conflicts with momentum, the injection wins.
