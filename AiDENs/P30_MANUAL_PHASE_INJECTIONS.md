# P30 Manual Phase Injections — v11B-centered

Paste the matching injection after each phase. Do not let Codex continue until it answers the injection with evidence paths and a go/no-go decision.

## Injection after P30-00

Stop. Revalidate source basis and target. Confirm this is now a v11B-centered pass, not a v11A-only pass. List the exact v11B surfaces this run will attempt, the v11A prerequisite blockers that must be closed first, and the surfaces that will remain explicit release debt. Do not proceed if source ownership is ambiguous.

## Injection after P30-01

Stop. Prove v11A prerequisites are not leaky. Show material operation receipts, deterministic ID policy, proof/degradation debt handling, boundary compiler status, and no-done-without-receipts tests. If any prerequisite is not satisfied, quarantine or downgrade all v11B claims.

## Injection after P30-02

Stop. Try to break parser, patch, rollback, and command paths. Show hostile fixtures for malformed tool calls, duplicate/ambiguous structured output, serialization failure, missing/unreadable patch targets, rollback failure, and timeout/sandbox behavior. No silent drop, no empty-success conversion, no ignored rollback.

## Injection after P30-03

Stop. Prove execution evidence is durable. Show receipts for provider/tool route, retry, failure, timeout, budget exhaustion, and queue/message lineage if present. If a failure influences output but has no receipt, stop and fix or quarantine.

## Injection after P30-04

Stop. Revalidate right-graph law. Show graph surface declarations and tests that prevent storage graph, retrieval graph, repair graph, subtraction graph, and control graph from silently acting as one graph. If any graph shortcut remains, record degradation or block promotion.

## Injection after P30-05

Stop. Prove region protocol exists beyond decorative structs. Show one region contract, one boundary message, one boundary receipt, one replay slice or non-replayability reason, and one state snapshot. Show reject/quarantine behavior for bad boundary input.

## Injection after P30-06

Stop. Break recursive/convergence behavior. Show normal convergence, non-convergence, oscillation, budget exhaustion, residual/syndrome emission, and local repair candidate behavior. Confirm failed convergence cannot emit stable-looking success.

## Injection after P30-07

Stop. Break lawful subtraction and causality. Show support core, removal frontier, invariant-preservation receipt, historical-loss budget, and challenge/rollback handling. Show causal/blame claims either use causal bundles or are explicitly degraded/refused.

## Injection after P30-08

Stop. Run the hostile sweep. Show grep/static guard output for panic, dynamic JSON misuse, silent degradation, `_ =`, broad `allow`, `unwrap_or_default`, stale docs, ambiguous root-current instructions, and gate drift. Fix or explicitly quarantine every release blocker.

## Injection after P30-09

Stop. Final hostile auditor handoff only. Do not claim v11B conformance unless full release bar is proven. Produce final release claims, unresolved risk ledger, command log, issue absorption report, v11B runtime spine report, and v11B conformance debt ledger. Every claim needs a receipt path.
