{
  "edits": [
    {
      "mode": "Modify",
      "ops": [
        {
          "Replace": {
            "lines": [
              "pub fn even_sum_1(values: &[u32]) -> u32 {",
              "    values.iter().copied().filter(|value| value % 2 == 0).sum()",
              "}",
              "",
              "#[cfg(test)]",
              "mod tests {",
              "    use super::*;",
              "    #[test]",
              "    fn sums_only_even_values() {",
              "        assert_eq!(even_sum_1(&[1, 2, 3, 4]), 6);",
              "    }",
              "}"
            ],
            "range": {
              "end_exclusive": 13,
              "start": 1
            }
          }
        }
      ],
      "path": "src/lib.rs"
    }
  ],
  "notes": [
    "evaluator-only oracle; excluded from learner public projection"
  ],
  "patch_id": "929ceef7-9962-51f0-8635-626f9c968e5e",
  "summary": "canonical evaluator patch for iterator-safety-01"
}
