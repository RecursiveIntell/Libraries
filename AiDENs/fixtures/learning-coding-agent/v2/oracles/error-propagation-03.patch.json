{
  "edits": [
    {
      "mode": "Modify",
      "ops": [
        {
          "Replace": {
            "lines": [
              "pub fn parse_positive_3(input: &str) -> Result<u32, String> {",
              "    let value = input.parse::<u32>().map_err(|_| \"invalid-integer\".to_string())?;",
              "    if value > 0 { Ok(value) } else { Err(\"not-positive\".into()) }",
              "}",
              "",
              "#[cfg(test)]",
              "mod tests {",
              "    use super::*;",
              "    #[test]",
              "    fn invalid_input_is_an_error() {",
              "        assert!(parse_positive_3(\"invalid\").is_err());",
              "    }",
              "}"
            ],
            "range": {
              "end_exclusive": 14,
              "start": 1
            }
          }
        }
      ],
      "path": "src/lib.rs"
    }
  ],
  "notes": [
    "evaluator-only oracle; excluded from learner public projection"
  ],
  "patch_id": "80f60bb9-28d7-52a4-895f-d46d7a20a0d8",
  "summary": "canonical evaluator patch for error-propagation-03"
}
